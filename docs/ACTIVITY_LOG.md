# Activity Log

## 2026-09-26 — HT UI: "Manager" → "Manadyè" everywhere (manager-access screens)
- Summary: after the "Manadjè" → "Manadyè" fix, 6 Kreyòl strings still used the English word "Manager" (manager-access request on /billing: title, description, reason label, pending, declined, already-granted). All 6 now say "Manadyè", so the whole HT UI and the HT newsletter use one term.
- Files touched: `src/lib/i18n.tsx` (6 HT values, lines 81-90), `CLAUDE.md`, `docs/ACTIVITY_LOG.md`
- Validation: lint PASS; tsc PASS; vitest 110/110 PASS; build PASS. Source scan: 953 HT strings, 0 "Manager", 0 "Manadjè", 8 "Manadyè"; no hardcoded `lang === "HT"` text mentions it. Live E2E (local build pointed at prod, test parent, UI switched to HT): /billing shows "Mande Aksè Manadyè", "Aksè Manadyè pèmèt…", "Rezon ki fè w ap mande aksè Manadyè"; no "Manager"/"Manadjè" on the page. Test account restored to en (6/6 profiles en/en).
- Note: a first E2E attempt reloaded the page immediately after switching language and came back in EN — the profile save had not finished, so the still-'en' profile was restored. Test-timing only (waiting ~2 s fixes it); a real user would only hit this by reloading within the same second.
- Risks + rollback: revert the commit (labels only).
- Blockers/human actions needed: none.

## 2026-09-26 — Newsletter HT rewritten (natural Kreyòl) + "Manadyè" spelling
- Summary: the HT version of `welcome-2026-10` was too literal. Rewritten in natural Haitian Creole (IPN spelling, no calques, proverb "men anpil, chay pa lou"), same meaning: 4 sections, 7 features, Mr A/reports in the subscription plans. Text approved by Dany, then "Manadjè" → "Manadyè". Row updated; status still `pending_approval`, NOT sent. UI i18n aligned: `role.manager` HT "Manadjè" → "Manadyè", `manager.dashboardTitle` HT "Tablo Bò Manadjè" → "Tablo Bò Manadyè".
- Files touched: `src/lib/i18n.tsx`, `CLAUDE.md`, `docs/ACTIVITY_LOG.md`; DB row `email_newsletter_drafts` (welcome-2026-10, HT).
- Validation: lint PASS; tsc PASS; vitest 110/110 PASS; build PASS. DB check: 0 "Manadjè", "Manadyè" present, no "ak ak"; 0 "Manadjè" left anywhere in src/.
- Risks + rollback: revert the commit for the UI labels; the previous HT draft text is in this session's transcript (not kept in the DB).
- Blockers/human actions needed: open question — 6 HT UI strings still use the English word "Manager" (manager-access request screens, i18n lines 81-90). Not changed; waiting for Dany's decision.

## 2026-09-26 — Language preference saved to the profile (FF5 blocker removed)
- Summary: the UI language lived only in localStorage (`im_lang`), and `profiles.language_pref` was "en"/"EN" for every account, so language-based sending (newsletter, FF5) could not work. Now:
  - DB (migration `20260926170000_profiles_language_normalize`): `normalize_language_code()` ('EN'->'en', 'pt-BR'->'pt', unknown->NULL) + BEFORE INSERT/UPDATE trigger `profiles_sync_language` that lowercases both columns and keeps `language_pref` = `preferred_language` (signup writes `preferred_language`, the app reads `language_pref`); invalid -> 'en'. CHECK `profiles_language_pref_check` on the 10 lowercase codes. The 6 existing profiles normalized to 'en' (all were en/EN, no other signal of their real language, so EN by default, no guessing).
  - App: new `LanguageProfileSync` (mounted in App): once per sign-in it applies the saved profile language (survives logout/login and new devices); afterwards every language change is saved to the profile in lowercase, whichever picker made it. A brand-new account still on the 'en' default with onboarding not done keeps the language picked before signup. `LanguageToggle` no longer writes the profile itself (it sent uppercase). Signup now passes `language` in the user metadata. Helpers + tests in `src/lib/languagePref.ts`.
- Files touched: `supabase/migrations/20260926170000_profiles_language_normalize.sql`, `src/lib/languagePref.ts`, `src/lib/languagePref.test.ts`, `src/components/LanguageProfileSync.tsx`, `src/components/LanguageToggle.tsx`, `src/contexts/AuthContext.tsx` (unreadable-profile fallback languagePref "" instead of "EN", so it never overwrites), `src/App.tsx`, `src/pages/Login.tsx`, `CLAUDE.md`, `docs/ACTIVITY_LOG.md`
- Validation: lint PASS (clean); tsc PASS; build PASS; vitest 110/110 PASS (+4). Trigger checked in a rolled-back transaction (HT->ht/ht, preferred FR->fr/fr, 'xx'->en/en). Live E2E against production DB (local build pointed at prod), test parent, 8/8 PASS: switch to HT -> exactly one PATCH `{"language_pref":"ht"}`, DB ht/ht; logout via UI button + local im_lang forced to EN + login -> UI in Kreyòl, DB still ht (stale local did not overwrite); fresh browser -> Kreyòl restored from profile; restored to en/en. Final: 6/6 profiles en/en.
- Note: the local `.env` still points at the old Supabase project (wkvatt…); Vercel uses prod. For local E2E, build with VITE_SUPABASE_* overridden to prod.
- Risks + rollback: revert the commit (app goes back to localStorage-only). DB: `ALTER TABLE public.profiles DROP CONSTRAINT profiles_language_pref_check; DROP TRIGGER profiles_sync_language ON public.profiles; DROP FUNCTION public.profiles_sync_language(); DROP FUNCTION public.normalize_language_code(text);` ('en' vs 'EN' is equivalent for every reader).
- Blockers/human actions needed: none. For the future send, match `lower(email_newsletter_drafts.language) = profiles.language_pref` (drafts use uppercase codes).

## 2026-09-26 — FF5/FF6 redesigned: article library + publishing calendar (no code)
- Summary: at the user's request, replaced the simple FF5 "weekly what's new email" in CLAUDE.md with the fuller architecture: stock of 5-10 generated drafts, admin "News/Articles" page (review, edit, approve, schedule, calendar view), automatic Saturday send of the next approved+scheduled article, 90-day expiry of unapproved drafts with automatic regeneration, sends in the recipient's language, and a public "News" archive (FF6 — not previously documented, folded in). Added guardrails (only approved+scheduled articles are ever sent) and codebase notes (existing email_newsletter_drafts schema, the profile-language blocker, LLM + fact base for generation, unsubscribe requirement). The Saturday send time was not specified in the original FF5 — left to decide.
- Files touched: `CLAUDE.md`, `docs/ACTIVITY_LOG.md` (docs only). Not started.

## 2026-09-26 — First newsletter drafted in 10 languages (NOT sent)
- Summary: created `email_newsletter_drafts` (id, campaign, language EN/HT/FR/ES/PT/AR/ZH/DE/JA/RU, title, content Markdown, status pending_approval/approved/sent/archived, created_at; unique (campaign, language); RLS: admins read/update only, service role writes, anon revoked) and stored campaign `welcome-2026-10`: a promotional presentation article (what homeschooling is + benefits; IME as an international platform in 10 languages; verified features only: personalized daily schedule, Mr A AI tutor answering in English + Haitian Creole, weekly progress reports, points/badges/rewards, a login per student, Co-Guardians with the same access as the parent, Managers for organizations; notes that Mr A + weekly reports are part of the subscription plans). 10 rows, all `pending_approval`; 378–498 words per language (ZH 950 / JA 1298 characters). Nothing was emailed.
- Distribution finding (to solve before sending): the UI language lives in the browser (localStorage `im_lang`) and is never written back to the profile — `profiles.language_pref` is 'en'/'EN' for all 6 accounts (mixed case), `preferred_language` 'en'. Sending "per profile language" today would give everyone the English version. Options: save the language to the profile when the user switches it (and normalize case), or ask for it at send time; fallback EN as specified.
- Files touched: `supabase/migrations/20260926160000_email_newsletter_drafts.sql` (applied live), `CLAUDE.md`, `docs/ACTIVITY_LOG.md`. Article content lives in the table (not in git).
- Risks + rollback: `DROP TABLE public.email_newsletter_drafts;`.
- Blockers/human actions needed: user review/approval of the 10 drafts; decide the language-sync fix before any send; sending itself needs explicit approval.

## 2026-09-26 — Future Features FF3-FF5 documented (no code)
- Summary: added to CLAUDE.md "Future Features" at the user's request: FF3 self-monitoring with a proposed-fix journal (proposals only, never auto-applied), FF4 multilingual user manual reachable from a Help menu link and kept up to date (the 4 PDFs EN/FR/ES/HT are not in the repo yet), FF5 weekly "what's new" email to parents/co-guardians/Managers (needs an opt-out; no unsubscribe tables exist yet). Objectives + high-level overview only.
- Files touched: `CLAUDE.md`, `docs/ACTIVITY_LOG.md` (docs only). Not started.

## 2026-09-26 — track-error: only the 4 critical pages alert admins
- Summary: `CRITICAL_PAGES = ['/', '/login', '/admin', '/admin/system']` was checked with `pagePath.startsWith(p)`; since '/' is in the list and every path starts with '/', every error on any page created a platform_error admin notification (30-min dedup per admin). List verified against the app routes (`/` dashboard, `/login`, `/admin` overview, `/admin/system`) and kept; now an exact match on a Set, with the trailing slash normalized ("/admin/system/" = "/admin/system"). The client sends `window.location.pathname`, so there is no query string.
- Files touched: `supabase/functions/track-error/index.ts` (deployed v4; deployed v3 verified identical to the repo before editing).
- Validation: lint PASS, `npx tsc --noEmit` PASS, build PASS, vitest 106/106. Live: errors on /e2e-noncritical, /admin/students, /login/extra, /loginx → 4 platform_errors logged, 0 notifications; /admin/system/ → 1 per admin (2); /login → 1 per admin (2). (A first run was invalid: Git Bash rewrote the "/path" arguments to "C:/Program Files/Git/…" — rows deleted, rerun with MSYS_NO_PATHCONV=1.) Test rows removed.
- Risks + rollback: revert the commit and redeploy the previous track-error.
- Blockers/human actions needed: none. Still not fixed (cosmetic): track-error reads `profiles.role`, which doesn't exist, so userRole in the message stays "anonymous".

## 2026-09-26 — Monitoring live: hourly-monitor, beta-track, dns-monitor (+ cron)
- Summary: the 3 functions were written but never deployed, and all 3 were out of sync with the schema. Stopped before applying (risk) and got 4 decisions from the user (all the recommended options): (A) drop hourly-monitor's legacy compliance part, keep the 5 admin alert rules; (B) fix the beta schema and deploy; (C) no alert on dns-monitor's first OK run; 30-day DNS history retention.
  - **hourly-monitor** (v1, verify_jwt=false, x-cron-secret): the legacy part sent Telegram nudges to students/parents every hour and auto-marked tasks "Missed", on columns that don't exist (students.monitoring_enabled, daily_plan.plan_date/start_time), so the function returned "No students" before ever reaching the admin rules. Rewritten to the 5 admin rules only, unchanged thresholds/dedup (error_spike 5+/page/h, low_rating, feature_trend, auth_failure_spike 10+/h, payment_failure); admin_notifications only. Cron `hourly-monitor-job` at :05 every hour.
  - **beta-track** (v1, verify_jwt=false, checks the user itself): code unchanged. Schema: beta_events CHECK widened with 'bug_report' + 'task_completion' (a batch containing a bug report used to be rejected entirely), 4 nullable scoring columns added to beta_task_completions. Note: the client never sends 'task_completion', so task_difficulty stays inactive.
  - **dns-monitor** (v2, verify_jwt=false, Authorization: Bearer secret, now fail-closed if the secret is unset): created dns_monitor_state / dns_monitor_history (admin read, service-role write; definitions from the never-applied 2026-07-07 migrations), first OK run = silent baseline, cron `dns-monitor-job` every 15 min, `dns-monitor-history-retention` daily (30 days). Email (BETA_ADMIN_EMAIL) / WhatsApp (ADMIN_WHATSAPP_NUMBER) are not configured → alerts are skipped (logged as skipped).
  - **Found and fixed during the test (my earlier DNS fix was fragile):** from the Supabase region, Google DoH returns 216.198.79.65/64.29.17.65 for the root, while Cloudflare DoH returned .1 — Vercel answers with different anycast IPs per resolver. Exact-IP checks would flag a healthy domain as misconfigured. `src/lib/dnsExpected.ts` and dns-monitor now accept any IP in Vercel's ranges (216.198.79.x, 64.29.17.x, 76.76.21.x); the wizard still shows the recommended values; the old Lovable IP stays rejected.
  - All crons read the secret from Vault (7 jobs, 0 literal secrets).
- Files touched: `supabase/functions/hourly-monitor/index.ts` (rewritten), `supabase/functions/dns-monitor/index.ts`, `supabase/migrations/20260926140000_monitoring_schema.sql`, `supabase/migrations/20260926140100_monitoring_cron.sql` (both applied live), `src/lib/dnsExpected.ts`, `src/lib/dnsExpected.test.ts`. beta-track source unchanged (deployed as-is).
- Validation: lint PASS (clean), `npx tsc --noEmit` PASS, build PASS, vitest 106/106. Live: A — 5 errors on one page → hourly-monitor 200 with error_spike for both admins, 2nd run deduped, wrong/no secret 401. B — temp beta tester: batch page_view+error+bug_report → 200, all 4 events stored (bug_report included), session upserted, last_active_at updated, beta_error + bug_report notifications for both admins, 2nd error deduped; non-tester 403, no session 401. C — wrong secret 401; both domains ok; baseline sends nothing; 2nd run unchanged. Cron path (Vault header) for both new jobs → 200. Production DNS panel: Check History populated ("ok … points to Vercel"), no 4xx. Test data removed (notifications, platform_errors, beta tester + events/session); the real DNS history rows are kept.
- Risks + rollback: rollback SQL at the end of both migrations (unschedule jobs, drop dns tables/columns, restore the CHECK); redeploy previous function versions from git history.
- Blockers/human actions needed: optional — set `BETA_ADMIN_EMAIL` (and Twilio + `ADMIN_WHATSAPP_NUMBER`) in Edge secrets if DNS change alerts should be sent. Found, not fixed: track-error's CRITICAL_PAGES contains '/', and `startsWith('/')` matches every path, so every error on any page creates a platform_error notification (30-min dedup per admin).

## 2026-09-26 — DNS Status false "Misconfigured" fixed (old Lovable IP hardcoded)
- Summary: the admin DNS Status panel expected the old Lovable A record `185.158.133.1` (hardcoded in 3 places), so it showed "Misconfigured" although the live Vercel setup is correct. Live DNS (DoH, 2026-09-26): root A = 216.198.79.1 + 64.29.17.1; www = CNAME 7d9278614535e49d.vercel-dns-017.com → 216.198.79.65 + 64.29.17.65 (root and www use different Vercel IPs).
  - New `src/lib/dnsExpected.ts` (single source for the UI): root OK = A records present and all Vercel root IPs; www OK = CNAME to the Vercel target (or, without CNAME, all Vercel www IPs). A stray non-Vercel A (e.g. the old IP left next to Vercel's) counts as misconfigured.
  - `AdminDnsStatus`: overall = root OK **and** www OK (was root only, against the old IP); shows the expected Vercel values; mismatch message names the failing record.
  - `DnsSetupWizard`: its instructions told admins to set A @ and A www to the old IP (following them would break the site) → now 2 root A rows + a www CNAME; checks use the shared logic.
  - `supabase/functions/dns-monitor`: same fix in source (own copy of the values; per-host rules). Not deployed — unchanged in production.
- Files touched: `src/lib/dnsExpected.ts` (new), `src/lib/dnsExpected.test.ts` (new, 6 tests), `src/pages/admin/AdminDnsStatus.tsx`, `src/components/admin/DnsSetupWizard.tsx`, `supabase/functions/dns-monitor/index.ts`.
- Validation: lint PASS (clean), `npx tsc --noEmit` PASS, build PASS, vitest 104/104. Browser (admin, same live DNS): production with the old code → "Misconfigured"; new code → "Resolving", expected values = Vercel, old IP nowhere on the page, Guided setup "All checks passing" (3/3 Verified), wizard says CNAME for www.
- Risks + rollback: revert the commit. If Vercel ever changes the project's DNS target/IPs, update `src/lib/dnsExpected.ts` (and dns-monitor).
- Blockers/human actions needed: none. Note: the panel's "Check History" says the monitor runs every 15 minutes, but dns-monitor is not deployed/scheduled, so the history stays empty (separate decision).

## 2026-09-26 — admin_notifications table created (admin panel 404 fixed)
- Summary: the admin panel (bell, sidebar alert count, notification center, beta page) and track-error / hourly-monitor / beta-track use `admin_notifications`, but the table was never created on this project (the 2026-03-22 Lovable migration was not applied) → 404. New migration (not the old file: it also adds an impersonation trigger on impersonation_logs columns that no longer exist, has no FK/indexes and grants UPDATE on every column).
  - Table: id, admin_id (FK auth.users, cascade), title (≤200), body (≤2000), notification_type, is_read (default false), metadata jsonb (default {}), created_at; indexes (admin_id, is_read, created_at desc) and (admin_id, notification_type, created_at desc).
  - RLS: SELECT and UPDATE only on own rows (admin_id = auth.uid() AND has_role admin); column grant UPDATE(is_read) only; no client INSERT/DELETE (edge functions via service role); anon revoked; added to the supabase_realtime publication.
  - Active trigger today: `platform_error` from track-error (errors on /, /login, /admin, /admin/system; 30-min dedup per admin). hourly-monitor (error_spike) and beta-track (bug_report/beta_error/task_difficulty) write it too but are not deployed (separate decision).
  - Frontend: AdminLayout alert count read `data.length` of a head:true query (always 0) → now `count`, and includes platform_error/error_spike (the active types). Realtime: the bell and the layout each opened a channel with the same binding; Realtime gives both the same subscription id and delivered each event to only one of them, so the bell never updated live. Now a single channel in AdminLayout re-broadcasts a window event the bell listens to.
- Files touched: `supabase/migrations/20260926120000_admin_notifications.sql` (applied live), `src/components/admin/AdminLayout.tsx`, `src/components/admin/AdminNotifications.tsx`.
- Validation: lint PASS (clean), `npx tsc --noEmit` PASS, build PASS, vitest 98/98. Live: grants/policies/publication verified in SQL. API: track-error on /login → 1 platform_error per admin, 2nd error within 30 min → no duplicate; parent reads 0 / insert denied / update 0 rows; admin client insert/delete denied, can't change title or admin_id (column grant), can't touch the other admin's rows; anon denied. Realtime via supabase-js: INSERT and UPDATE events delivered under RLS. Browser (admin): bell 1 → "Mark all read" → 0 and is_read=true in DB → new error → bell 1 live without reload; sidebar Notifications badge 1; no 4xx on admin_notifications. Test rows removed (0 notifications, 0 test platform_errors).
- Risks + rollback: rollback SQL at the end of the migration; revert the commit for the UI.
- Blockers/human actions needed: none. Found, not fixed: `inbox-badge` Realtime channel fails to subscribe ("Unable to subscribe to changes with given parameters" — inbox_messages not in the Realtime publication); track-error reads the nonexistent `profiles.role` (cosmetic userRole in the message).

## 2026-09-26 — FF2 flow decision recorded (no code)
- Summary: user decided Option 1 for the future affiliate store: points are debited only on parent/Manager approval, not on the student's request. Recorded in CLAUDE.md FF2 with the implementation outline (split redeem_reward into request = no points touched, approval = balance re-check + debit under the row lock; add a decline status; handle legacy pending rows that were debited at request) and the reasoning.
- Files touched: `CLAUDE.md`, `docs/ACTIVITY_LOG.md` (docs only). Not implemented.

## 2026-09-26 — Future Features documented (no code)
- Summary: added a "Future Features" section to CLAUDE.md at the user's request: FF1 marketing/showcase site on www (Homeschool Platform, Tutoring, Cours Privé) with the platform moved to an app. subdomain; FF2 affiliate/dropshipping store extending rewards_catalog (image, affiliate link, USD reference price → points; student requests via redeem_reward, parent/Manager approves and buys manually; IME earns commission only). Each entry lists the codebase impacts found (hardcoded www URLs in ~24 edge functions, Auth/OAuth/Stripe redirect settings; redeem_reward debits at request time with no reject/refund today).
- Files touched: `CLAUDE.md`, `docs/ACTIVITY_LOG.md` (docs only). Not started — to be planned separately.

## 2026-09-26 — Weekly Progress Report: check-ins, badges, points back (column + value mismatches)
- Summary: `weekly-report-data` queried columns that don't exist and silently turned every failed query into an empty list (`res.data || []`), so check-ins, badges and points were empty for everyone; the UI also compared values in the wrong format, so completion was always 0%.
  | Section | Code asked for | Real schema | Kind |
  |---|---|---|---|
  | Check-ins | `check_ins.timestamp`; UI mapped mood/focus from emoji | `checked_in_at`; mood 'focused'/'okay'/'struggling', focus 1-5 | rename + value format |
  | Badges | `achievements.name, type, criteria_met_at` | `badge_type, milestone, earned_at` (badges system, `lib/badges.ts`) | structure change |
  | Points | filter `reward_points.created_at`; UI summed all rows | `awarded_at`; redemptions are negative debits | rename + semantics |
  | Tasks | UI `status === "Done"`, ReportsPanel filter `status: "Done"` | stored lowercase 'done' | value |
  Proof: the 3 queries returned 42703 ("column … does not exist") as the parent; `status='Done'` → 0 rows.
- Fix (code only, no schema change): the function selects/filters/orders on the real columns, lowercases the status filter (old "Done" clients still work) and returns 500 + log instead of empty sections when a query fails; ownership/subscription logic unchanged. WeeklyProgressReport: done = 'done', mood focused=5/okay=3/struggling=1, focus 1-5 as-is, badges shown with translated names (`badge.<badge_type>` + emoji), "points earned" = positive rows only. ReportsPanel: 'done' everywhere.
- Files touched: `supabase/functions/weekly-report-data/index.ts` (deployed v5), `src/components/WeeklyProgressReport.tsx`, `src/components/ReportsPanel.tsx`.
- Validation: lint PASS (clean), `npx tsc --noEmit` PASS (strict count unchanged at 395, 0 in touched files), build PASS, vitest 98/98. Real E2E: seeded this week for a temp student through the real paths as the parent (3 tasks, 2 marked done → task_done trigger +10 each; award_points +40 → automatic points_50 badge; 2 check-ins; a 15-pt redemption via redeem_reward). API: 200, 3 tasks/2 done, 2 check-ins with checked_in_at/mood/focus, points_50 badge, positive points 60 (raw 45 with the debit), legacy "Done" filter → 2, other family 403. Browser (parent, Reports tab): 67% (2/3), 60 points earned, 1 badge "🌱 Starter", daily bars Tue/Wed done Thu missed, subjects Math/Science, mood & focus trend lines; 0 4xx/5xx. Test data removed (0 left, no orphans).
- Risks + rollback: revert the commit and redeploy weekly-report-data from the previous commit (v4 code = `9a658f8`).
- Blockers/human actions needed: none.

## 2026-09-26 — CLOSED: server-side password rules (Supabase Free plan limit)
- Summary: dossier closed at the user's decision. Leaked-password protection (HaveIBeenPwned) and the server-side minimum password length are not enforced on this project: live tests on 2026-09-26 showed the Auth server accepting a 7-character and a known-pwned password, and the security advisor still reports leaked-password protection disabled. The user confirmed the project is on the real Supabase Free plan and that both are plan limits, not a bug or misconfiguration.
- Status: **Limit konfime plan Supabase Free — mande upgrade Pro pou rezoud.** No further technical action is possible without changing plan.
- Mitigation in place: client-side minimum of 8 characters on signup and reset-password (commit `93cfae4`).
- On upgrade to Pro: enable both settings in Authentication, then re-run the live check (7-char and pwned password must be rejected by PUT /auth/v1/user).
- Files touched: `CLAUDE.md`, `docs/ACTIVITY_LOG.md` (docs only).

## 2026-09-26 — Client-side minimum password length 8
- Summary: the Supabase dashboard minimum-length setting is not enforced by the server yet (live test: a 7-char password is accepted by PUT /auth/v1/user; checked twice today), and Leaked-password protection needs the Pro plan (project is on Free — confirmed by the user; the advisor still reports it disabled). Added a client-side second layer: shared `MIN_PASSWORD_LENGTH = 8` (`src/lib/password.ts`) used by ResetPassword (was 6) and the signup form (had no length check at all); `auth.passwordTooShort` now says 8 in all 10 languages.
- Files touched: `src/lib/password.ts` (new), `src/lib/password.test.ts` (new), `src/pages/ResetPassword.tsx`, `src/pages/Login.tsx`, `src/lib/i18n.tsx`.
- Validation: lint PASS (clean), `npx tsc --noEmit` PASS, build PASS, vitest 98/98 (+2). Real-browser E2E (local UI against prod Supabase): signup with a 7-char password → "Modpas dwe gen omwen 8 karaktè", no /auth/v1/signup request sent; /reset-password with a 7-char password → "Password must be at least 8 characters", no PUT /auth/v1/user sent. Server check: 7 chars still accepted (test account password restored each time, login 200).
- Risks + rollback: revert the commit. Client checks can be bypassed by calling the Auth API directly — the server setting is still needed.
- Blockers/human actions needed: (1) In the dashboard, confirm the minimum password length shows 8 **and was saved**; if it shows 8 but the server still accepts 7 chars, report it to Supabase support. (2) Leaked-password protection: requires upgrading to Pro.

## 2026-09-26 — Commit 08-30 diagnostic doc; ignore supabase/.temp
- Summary: committed `docs/DIAGNOSTIC_2026-08-30.md` (historical Supabase failure-rate diagnostic) at the user's request. Before committing to the public repo it was scanned for secrets/PII: one retired cron bearer token was quoted in clear (line 63) — redacted; verified it is used nowhere (0 in cron.job, 0 in SQL functions, 0 in code) and never was in git history. No emails/keys/IDs otherwise. Added `supabase/.temp/` (Supabase CLI local state) to `.gitignore`.
- Files touched: `docs/DIAGNOSTIC_2026-08-30.md` (new, 1 line redacted), `.gitignore`, `CLAUDE.md`, `docs/ACTIVITY_LOG.md`.
- Validation: docs/config only; `git status` clean afterwards (`.temp` ignored).
- Risks + rollback: revert the commit.
- Blockers/human actions needed: none.

## 2026-09-26 — Task 1: reminder functions committed (keep, not discard)
- Summary: the 4 cron functions (morning-reminder, checkin-reminder, daily-report, weekly-badge) had been live since 2026-08-30 (v4/v5, rewritten from the legacy single-student "CHRIS"/Telegram code to multi-family Resend email with the x-cron-secret guard) but were never committed, so the repo still held the legacy code and `npm run lint` failed on them. User chose "keep and commit". Only change: the 4 `result.ok ? sent++ : failed++;` expression statements became `if (result.ok) sent++; else failed++;` (no behavior change).
- Verified before committing: deployed source of all 4 matches the working copy (morning-reminder line by line; the other 3 by their distinctive lines, same line counts). Live since 09-08: 19 `sent` / 0 `failed` each for morning/checkin/daily; weekly-badge sends only with weekly progress. Functions not redeployed (the only diff vs production is the no-op lint form); the repo is now the source of truth for the next deploy.
- Files touched: `supabase/functions/{morning-reminder,checkin-reminder,daily-report,weekly-badge}/index.ts`.
- Validation: `npm run lint` PASS (exit 0 — first fully clean lint), `npx tsc --noEmit` PASS, build PASS, vitest 96/96.
- Risks + rollback: revert this commit (repo goes back to the legacy code, which must NOT be deployed).
- Blockers/human actions needed: only the Auth dashboard settings (leaked-password protection, min length 8).

## 2026-09-26 — CRON_SECRET rotated; cron jobs read it from Vault
- Summary: the user set a new CRON_SECRET in Edge Function secrets. Stored the same value in Supabase Vault (`cron_secret`) and rewrote the 4 cron.job commands (morning-reminder, checkin-reminder, daily-report, weekly-badge) to read the `x-cron-secret` header from `vault.decrypted_secrets` at run time, so no secret value lives in cron.job, migrations or git anymore. Schedules unchanged. Next rotation = update Vault + Edge secret only.
- Files touched: `supabase/migrations/20260926090000_cron_secret_from_vault.sql` (applied live; contains no secret value). Vault secret created via SQL (not in any file).
- Validation (real curl): OLD (publicly leaked) secret → 401 on all 4 functions; no secret → 401; NEW secret → 200 on weekly-badge (`sent:0, noProgress:3` — chosen because no active family had progress this week, so no email went out; the other 3 functions send to every active parent, including a real Manager, so they were only tested for 401). Executing the job's own Vault-reading command through pg_net → 200 (request #86). cron.job: 4/4 read Vault, 0 literal secrets.
- Risks + rollback: if Vault and the Edge secret ever differ, all 4 jobs get 401 (visible in net._http_response). Rollback: `cron.alter_job` back to a literal header.
- Blockers/human actions needed: none. The old value is still in public git history but is now useless.

## 2026-09-25 — Co-guardian invite fixed end to end (Task 2)
- Summary: the "Invite Co-Guardian" button never worked: send/accept-guardian-invite were not deployed and were written for a schema that doesn't exist (student_id, invitee_email, permissions jsonb, token_hash, invited_by, accepted_at). Rebuilt on the real family-level schema with the 4 approved decisions: (1) only the primary parent (owns a student) invites; (2) only the invited email can accept; (3) co-guardian = same access as the parent (the 4 permission toggles had no effect in RLS and were removed from the UI); (4) the family's subscription covers the co-guardian.
  - send-guardian-invite: copy-a-link, no automatic email. Existing account that has signed in → `/accept-invite?token=…`; new account (or never signed in) → generateLink invite/recovery that lands on `/reset-password?next=/accept-invite…` (set password, then accept). Reuses a pending invite for the same email; refuses self, existing co-guardian, student accounts, invalid email; non-owners 403.
  - accept-guardian-invite: invalid token 404, used 409, revoked/expired 410, other email 403, own invite 400, student account 403; creates the co_guardians row (full access), ensures the 'parent' role, marks the invite accepted (accepted_at/accepted_by).
  - Security (was deferred from the audit): parents could INSERT any user id into co_guardians → now server-only; parent keeps SELECT + DELETE; invites SELECT + revoke-only UPDATE; anon grants revoked. New `get_my_co_guardians()` (names/emails; profiles RLS hides other users).
  - Access: AuthContext now loads the co-guarded families' students (RLS already allowed it; the client filtered on parent_id = me). ai-tutor and weekly-report-data used `parent_id = caller` + the caller's own subscription → co-guardians AND Managers got 403; now `get_managed_parent_ids` + the student's family subscription. `get_my_effective_subscription` falls back to a co-guarded family.
  - ResetPassword accepted only type=recovery, so every `type=invite` link (student logins from create-student-account, manager-created parents) landed on "invalid link"; now accepts invite links and a same-origin `next`.
- Files touched: `supabase/migrations/20260925150000_co_guardian_invites.sql` (applied live), `supabase/functions/{send-guardian-invite,accept-guardian-invite}` (new v1, verify_jwt), `supabase/functions/ai-tutor` (v6), `supabase/functions/weekly-report-data` (v3), `src/components/{CoGuardiansPanel,StudentCoGuardiansSection}.tsx`, `src/contexts/AuthContext.tsx`, `src/pages/{AcceptInvite,ResetPassword}.tsx`, `src/lib/i18n.tsx` (38 keys × 10). Commit `9a658f8` → Vercel `dpl_7kr7YQbSjqvZwEQJiWPGPiZa2oBo` READY (production, www).
- Validation: lint clean on touched files (repo lint only fails on the 4 frozen reminder fns — Task 1 still pending); `npx tsc --noEmit` PASS (strict count 424 → 395); build PASS; vitest 96/96. Real E2E (production build of this branch served on the real www domain via Playwright routing, production Supabase): parent clicks "Create invite link" in the UI → link + "create a password first" note; new guardian opens it → set-password form → /accept-invite → "You're now a co-guardian!" → Parent Dashboard showing the family's student; parent's list shows the guardian. Access with the guardian session: reads the 2 students, adds a task, marks it done (+10 points trigger), reads check-ins/points, effective subscription = family's active plan, weekly-report-data 200, ai-tutor 200 (other family 403, parent still 200). Negatives: reuse 409, already co-guardian 409, self 400, invalid email 400, non-owner 403, other email 403, revoked 410, expired 410, unknown token 404, student account 400, parent direct INSERT → RLS error, parent can't flip an invite to accepted, anon denied. Student invite link (type=invite) now shows the set-password form and lands on the dashboard. Parent removes the guardian in the UI → guardian reads 0 students, ai-tutor 403, no subscription. Production smoke: HT panel renders, 0 4xx, bad token → localized message. All test data removed.
- Risks + rollback: revert `9a658f8`; migration ends with rollback SQL; redeploy previous ai-tutor/weekly-report-data from git history (`ede0ed9`). A brand-new co-guardian sees the generic onboarding modal ("Add Student") on first login — cosmetic.
- Blockers/human actions needed: none for this task. Still pending from earlier: Task 1 decision (reminder fns), CRON_SECRET rotation, leaked-password protection. Found, not fixed: weekly-report-data queries nonexistent columns (check_ins.timestamp, achievements.name/type/criteria_met_at, reward_points.created_at) → those sections come back empty.

## 2026-09-25 — Security audit (full platform) + fixes
- Summary: audit of RLS (all public tables), SECURITY DEFINER functions, edge functions (deployed code, not just repo), secrets, storage, HTTP headers, Stripe, frontend XSS/redirects, Auth data. Session-only exception granted by the user: fixes pushed straight to main after each live test (CLAUDE.md "branches, never direct to main" rule stays in force for every other session). Tasks 1 (reminder fns) and 2 (co-guardian) left untouched as instructed.

| # | Severity | Finding | Fix | Proof (live) | Commit |
|---|---|---|---|---|---|
| 1 | CRITICAL | Deployed `test-faz1-fixture` (verify_jwt=false) guarded only by CRON_SECRET, which is public on GitHub. Its `cleanup` action deletes ANY user's students/data/subscription and calls `auth.admin.deleteUser` (admins included); `setup` grants a free active subscription. | Redeployed as a 410 stub with verify_jwt=true (no MCP delete tool). | Before: public secret → 400 "unknown action" (auth passed). After: 401 without JWT, 410 with JWT+secret. Edge logs (24h): no abuse seen. | deploy only (v3) |
| 2 | HIGH (pending user) | CRON_SECRET value committed in the public repo (ACTIVITY_LOG + 08-30 cron migration) and still active → anyone can trigger the 4 cron fns (email blast to every active parent). | Value redacted from current files; rotation approved: user sets new secret in Edge secrets + Vault `cron_secret`, then cron.job reads Vault. | `cron.job` confirmed using the public value. | `8e56148` (redaction) |
| 3 | HIGH | `ai-tutor` 30/h cap called `increment_rate_limit`, which never existed → cap failed open (unlimited paid OpenAI calls). | Created the RPC on the real `rate_limits` table + unique index; service_role only. | SQL: call 30 allowed, 31 refused. Real ai-tutor: 200 (remaining 29); at count 30 → 429. | `66d59ee` |
| 4 | MEDIUM | No clickjacking/nosniff/referrer/permissions headers (only HSTS). | vercel.json: X-Frame-Options DENY, CSP `frame-ancestors 'none'; object-src 'none'; base-uri 'self'`, nosniff, Referrer-Policy, Permissions-Policy. | Headers live; app refuses to render in a cross-origin iframe; login/dashboard OK, no JS errors. | `f4cff45` |
| 5 | MEDIUM | `track-auth-failure` (public) inserted one row per request, no limit → table flooding. | Max 20 / 10 min per masked IP + 300 / 10 min global, still 202; index. | 25 rapid calls → 25×202, 20 stored; real wrong-password login still recorded. | `51994ab` |
| 6 | LOW | `has_role(uid, role)` and `get_managed_parent_ids(uid)` answered for any uid (who is admin/manager, other users' families). | Answer only for auth.uid() (or server calls with no auth.uid()). | Other uid → false / []; self → true; admin panel + RLS reads OK. | `477acca` |
| 7 | LOW | 10 trigger functions exposed as /rpc endpoints to anon/authenticated. | REVOKE EXECUTE (triggers unaffected). | RPC calls refused; task→points and check-in stamp triggers still fire. | `477acca` |
| 8 | LOW | Parent could set `students.user_id` to any account. | Trigger: only server-side linking; unlink allowed. | Link to other account refused (update + insert); create-student-account still links; student logs in. | `477acca` |
| 9 | LOW | `student_duplicate_exists` = unlimited cross-family "is this child here" oracle. | 30 checks / hour / user. | 31st → rate_limited. | `477acca` |
| 10 | LOW | `student-photos` bucket: any file type/size. | Images only (jpeg/png/webp/gif/heic/heif), 10 MB. | HTML upload rejected; PNG upload OK. | `477acca` |
| 11 | INFO | `.env.test` (test passwords) force-tracked in the public repo. | Untracked (.gitignore already has `.env*`). Committed passwords verified dead (400). | — | `8e56148` |

- Reviewed and OK: RLS enabled on every public table, no `USING (true)` on user data; Stripe webhook signature verified, price/redirect URLs server-side; portal by own customer only; admin RPCs check has_role; frontend innerHTML sinks use escaped/React-rendered content; `next`/`redirect` params stay same-origin; storage policies family-scoped; all SECURITY DEFINER fns pin search_path; remaining 11 authenticated-executable definer RPCs each check the caller; 2 admins = owner + test-admin (expected); 0 unconfirmed sign-ins.
- Accepted / not fixed: `pg_net` in public schema (moving it risks the cron jobs; low); INFO "RLS no policy" on password_reset_requests / student_account_invites (intended, service_role only); `beta_requests` public INSERT (spam only, low); `handle_new_user` matches pending student invites by email at signup (invites are consumed in the same request; theoretical).
- Deferred to Task 2 (frozen): parent can INSERT co_guardians for any user id; anon grants on co_guardians/guardian_invites (RLS blocks).
- Found, not security: `ai-tutor` doesn't persist conversations (ai_conversations shape mismatch); `admin_notifications` table missing (404 in admin panel); `BlockReminderPopup` legacy columns (400); test parent has a subscription row with no Stripe customer (portal 404).
- Validation: `npx tsc --noEmit` PASS, build PASS, vitest 96/96; lint only fails on the 4 frozen reminder fns. Advisors: anon-executable SECURITY DEFINER 3 → 0, authenticated 14 → 11 (all intended). All test data removed.
- Risks + rollback: each migration file ends with its rollback SQL; revert the listed commits; track-auth-failure v2 code is the previous repo version.
- Blockers/human actions needed: (1) **Rotate CRON_SECRET** (Edge secret + Vault `cron_secret`, then tell Claude to switch cron.job to Vault). (2) Supabase Auth → enable **Leaked password protection** and set minimum password length ≥ 8 (not reachable via MCP). (3) Optional: delete `test-faz1-fixture`, `monitor-create-parent`, `review-monitor-request` from the dashboard (all inert stubs now). (4) Custom SMTP (Resend) for Auth emails (see password-reset entry).

## 2026-09-25 — Rewards shop + schedule templates fixed (column mismatch → real schema, server-side redeem)
- Summary: approved plan applied. Before: 171 HTTP 400s/24h (reward_redemptions 158, rewards_catalog 10, schedule_templates 3); shop, redemptions and templates never worked; `redeem_reward` RPC did not exist; the client sent the price.
  - `rewards_catalog` stays per family (parent_id/title/is_active); `useRewards` maps to the UI's name/enabled; new `icon` column. Parent/Manager/co-guardian insert with the student's family id (`useStudentFamilyId`).
  - `reward_redemptions`: new `points_spent` + `reward_title` snapshot columns; UI uses requested_at/approved_at and status pending → redeemed (Fulfill).
  - New `redeem_reward(_student_id, _reward_id)` SECURITY DEFINER: price from the catalog only, balance = SUM(reward_points), redemption + negative `reward_points` row ('redemption', reference_id = redemption) in one transaction under `SELECT … FOR UPDATE` on the student row. Caller = student's own account or whoever manages the family. Errors: insufficient_points / reward_unavailable / forbidden → localized toasts (were swallowed before).
  - New SELECT policies: student reads family catalog + own redemptions.
  - `ScheduleTemplates`: family templates (dropped is_builtin order + student_id insert), save/apply/copy-last-week on daily_plan's real columns via `lib/dailyPlan` (times stay in title), local dates (no UTC day shift).
  - **Security bug found by the E2E and fixed before commit:** first version of the permission check was `IF NOT (user_id = auth.uid() OR …)`; for a sibling (user_id NULL) that is `NOT (NULL OR false)` = NULL → no exception → a student could spend a sibling's points. Now `COALESCE(…, false)` (migration `redeem_reward_null_safe_permission`), retested with a sibling holding 500 points → forbidden.
- Files touched: `supabase/migrations/20260925090000_rewards_shop_redeem.sql` (applied live: `rewards_shop_redeem` + `redeem_reward_null_safe_permission`), `src/hooks/useRewards.ts`, `src/components/{RewardsPanel,RewardsManagement,ScheduleTemplates}.tsx`, `src/lib/i18n.tsx` (7 keys × 10 languages). Commit `0bf1b30` → Vercel `dpl_5tFcA73PNbEXa1yzuMMWN2KCDsby` READY, production, www.
- Validation: eslint clean on touched files (repo lint only fails on the 4 uncommitted reminder fns — pending decision); `npx tsc --noEmit` PASS (app-config strict count 450 → 424); build PASS; vitest 96/96. Live E2E (local UI against prod Supabase + API): parent creates a reward in the UI dialog → stored as family row; student account (real login) sees only own family's active rewards, redeems in the UI (toast, "E2E Movie night · 60 pts / Ap tann", balance 100 → 40); RPC: insufficient_points, inactive/other-family → reward_unavailable, sibling (with 500 pts) + other family + anon → refused; student direct inserts into reward_redemptions/reward_points → RLS error; student can't change price (0 rows); 3 parallel redeems at balance 100 → exactly 1 success, balance 40; parent Fulfill → status redeemed + approved_at; parent Save Template / Apply Template / Copy Last Week → correct daily_plan rows (dates, titles, status planned). 0 4xx on the 3 tables during tests; production read-only smoke: 200 on all 3. All test data removed (0 students/catalog/redemptions/templates/users, test parent back to 1 student).
- Risks + rollback: revert `0bf1b30`; SQL rollback at the end of the migration file (drop function, 2 policies, 3 columns). Parent "reject + refund" for a redemption does not exist (status check has no rejected value) — not in scope.
- Blockers/human actions needed: (1) CRON_SECRET rotation approved — waiting for the user to set the new value in Edge Function secrets + Vault `cron_secret`, then cron.job moves to Vault. (2) Decisions pending: reminder fns (keep + commit recommended), co-guardian 4 decisions. (3) Found, not fixed: `BlockReminderPopup.tsx` still selects legacy daily_plan columns (start_time/end_time/block_order/plan_date) → 400 on the student page.

## 2026-09-25 — Password reset for student accounts + 15-min reset limit for every account
- Summary: two reset paths, both throttled by one shared server-side limit (1 request per account every 15 min), checked BEFORE Supabase Auth is called.
  - Path A (parent/Manager): "Reset password" button on the student login card (shown when the student already has a login; card now also shown to Managers, server re-checks). New fn `reset-student-password`: same gate as create-student-account (`can_impersonate_student`, admins 403), `generateLink('recovery')` → link shown for copy (no email sent). requested_by = parent if caller is students.parent_id, else manager.
  - Path B (everyone: parent, Manager, co-guardian, student): the existing single "Forgot password?" form (/forgot-password, shared by all roles — there was no separate form) now calls new public fn `request-password-reset` (verify_jwt=false) instead of `resetPasswordForEmail` from the browser. Unknown emails get the same success and send nothing / record nothing (no account enumeration).
  - `password_reset_requests` (RLS on, 0 policies, service_role only) + `claim_password_reset()` (advisory lock per user → atomic check+insert) + `find_auth_user_id_by_email()` (service_role only). Slot released if the Auth call fails for a non-rate-limit reason; kept if Auth itself rate-limits.
  - UI never shows raw Auth errors: localized "Tanpri tann X minit anvan w mande yon lòt lyen" (10 languages, 6 new keys).
- Files touched: `supabase/migrations/20260924190000_password_reset_requests.sql` (applied live via apply_migration), `supabase/functions/reset-student-password/index.ts` (new, deployed v1, verify_jwt=true), `supabase/functions/request-password-reset/index.ts` (new, deployed v1, verify_jwt=false), `supabase/config.toml`, `src/components/StudentLoginInvite.tsx`, `src/components/StudentProfileCard.tsx`, `src/pages/ForgotPassword.tsx`, `src/lib/i18n.tsx`. Commit `7df2a43` → Vercel `dpl_FiajuGDphxZfeKpjwSciEPfBy7tL` READY, production, aliased www.independentmindsedu.org.
- Validation: eslint clean on touched files (repo-wide lint still fails only on the 4 pre-existing uncommitted cron fn edits, not part of this commit); `npx tsc --noEmit` PASS (app-config strict count unchanged at baseline 450); build PASS; vitest 96/96. Grants verified live (only service_role/postgres). Live E2E on production: A 409 without login, 401 no session, 403 admin/other family, 200 + recovery link → 303 /reset-password#type=recovery → new password set → student login OK; 2nd request 429 (retry 899s); 5 simultaneous clicks → exactly one 200 + four 429; shared limit A→B and B→A both 429; B unknown email 200 + 0 rows; invalid email 400; B real email sent (auth log mail.send recovery 04:12:17Z) with "check your email" screen on production UI; 2nd B attempt → HT message "Tanpri tann 15 minit…"; parent UI throttled toast + link box verified in real browser. 15 min simulated by back-dating rows. All test data removed (0 users/students/rows/invites).
- Risks + rollback: revert `7df2a43` (restores direct resetPasswordForEmail); delete the 2 functions; `DROP FUNCTION find_auth_user_id_by_email(text); DROP FUNCTION claim_password_reset(uuid,uuid,text,uuid); DROP TABLE password_reset_requests;`. Anyone can still call /auth/v1/recover directly with the public anon key (bypasses our table; Supabase's own limit still applies).
- Blockers/human actions needed: **Root cause of today's rate-limit hits found:** Auth emails go out through Supabase's built-in mailer (`mail_from: noreply@mail.app.supabase.io`), NOT Resend/auth-email-hook — capped at ~2 emails/hour for the whole project (1st path B attempt was refused by it at 03:44 and 03:49 after only 2 sends since 03:02). Fix in the Supabase dashboard (not available via MCP): Authentication → Emails → SMTP Settings → enable custom SMTP with Resend (smtp.resend.com, sender noreply@independentmindsedu.org) or enable the Send Email Hook → auth-email-hook; then raise Authentication → Rate Limits → "emails per hour". The recovery email itself was not clicked from the inbox (Gmail connector needs re-auth); its link uses the same /verify → /reset-password mechanism verified end to end in path A.

## 2026-09-24 — Rewards system (points + badges) + Done button fixed end to end
- Summary: built the rewards system from the spec below, following the approved plan. All points and badges are computed server-side by triggers; clients can't insert into `reward_points` or `achievements`.
  - The Done button now works. Four breakages were fixed: the `useDailyBlocks` query (400), the "Done" vs `done` status mismatch, the ghost columns, and the missing `award_points`. The status and title mapping lives in one place, `lib/dailyPlan.ts`.
  - Also fixed on the same flow: the parent "Add block" / edit / CSV and today views (they used the same nonexistent columns, so parents could not create tasks), the check-in form mapping (`need_help`/`comment` → `help_needed`/`note`, etc.; without it "Fidèl" was unreachable), the ActivityFeed filter, and StudentStatsBar (`plan_date` 400).
  - Security extra: client check-ins are stamped `now()` server-side. Before, a student could backdate `checked_in_at` and fake the 7-day streak.
  - Legacy client bonuses (check-in, streak, perfect day, 5/5, challenge, category) are disabled: no caller is left and the hooks are kept for later. `useCheckAndAwardBadges`, which wrote the wrong achievements columns, was removed.
- Files touched:
  - `supabase/migrations/20260924170000_rewards_points_badges.sql` (applied live)
  - New: `src/lib/dailyPlan.ts`, `src/lib/badges.ts`, `src/hooks/useStudentRewards.ts`, `src/components/RewardsSummaryCard.tsx`
  - Edited: `src/hooks/{useDailyBlocks,useAchievements,useSubmitCheckIn,useRewards}.ts`, `src/lib/syncManager.ts`, `src/components/{TodayBlocks,DadPanel,PointSettingsPanel,ActivityFeed,StudentStatsBar}.tsx`, `src/pages/Index.tsx`, `src/pages/admin/AdminStudents.tsx`, `src/lib/i18n.tsx` (14 new keys × 10 languages)
- Validation:
  - `npx tsc --noEmit` PASS. `npm run build` PASS. vitest 96/96 PASS. eslint on all touched files PASS. Strict `tsconfig.app.json` errors went from 466 to 453, none new.
  - **Live API E2E: 36/36 PASS.** Real test parent, a temporary student login created through `create-student-account`, and test-admin, all on production:
    - Loading blocks returns 200 (the old query still returns 400). Writing `"Done"` or a ghost column is rejected. Start → `started` gives no points; Done → +10 (default). Re-marking done does not pay twice.
    - Student: `award_points` → 403; direct INSERT into `reward_points`/`achievements` → 403; cannot change the parent's setting.
    - Parent: sets 25 (5000 rejected) → +25 per task. Total 60 awards Kòmanse. Parent `award_points` +100 → 160 awards Bronze. `source=task_done` is refused.
    - A backdated check-in is re-stamped to today. 7 consecutive days awards Fidèl.
    - Admin summary shows 160 pts + 3 badges. Parent calling the summary → 403. Admin `award_points` → 403. Admin direct achievements read → 0 rows.
  - **Real browser (Playwright, production www):**
    - Student Mark Done: 160→185 (+25). The PATCH body was `{"status":"done",...}`. The card shows Starter/Bronze/Loyal and "Next badge: Silver 185/400".
    - Parent: Schedule "Add block" (POST uses the real columns) and "points per task" 25→10 saved. Then the student Start→Done on that UI-created block: 185→195 (+10), with no rewards-related REST errors.
    - Admin /admin/students shows 195 + 3 badges and no "View as" button.
    - The browser run found a race in my first PointSettingsPanel (the input showed default 10 while loading, then the late load overwrote the typed value). It is fixed in `e00bf71` and re-verified.
  - All test data cleaned: 0 `daily_plan`/`reward_points`/`achievements`/`check_ins` rows for Test Student. The test parent's `parent_settings` row (created by the test) is deleted. The temporary student auth user, role, profile and invite are deleted. `Test Student.user_id` is back to null.
- Risks + rollback: the migration footer lists the DROP order (functions/triggers/indexes/3 columns; no existing data was modified). Revert commits `65ea427` and `e00bf71` for the client.
- Blockers/human actions needed / noted for later (not touched, per decision 2):
  - The rewards shop (`rewards_catalog`/`reward_redemptions`/`RewardsPanel`/`RewardsManagement`) has the same column-mismatch class (400s).
  - `schedule_templates` returns 400 (ScheduleTemplates also writes legacy `daily_plan` columns).
  - `challenges`, `currency_settings`, `point_settings` and `admin_notifications` return 404.
  - Login inputs and the parent hamburger menu have no accessible labels.
  - "Days Left" uses a hard-coded 2026-07-03 date.
  - Noticed during the test: Christian's student login was created at 15:46 by the parent account `augustindany@hotmail.com` (the user's own action, not test data; left untouched).

**Spec (user, verbatim, HT):**
> Sistèm Rewards (pwen + badj) — konstwi nèf, pa yon bug ki dwe korije. Kontèks: bouton "Make tach la fèt" te kraze paske li rele yon RPC award_points ki pa egziste. Nou vle bati sistèm Rewards konplè a kounye a.
> PWEN
> - Konplete yon tach (daily_plan status → done) bay X pwen (default template: 10 pwen pa tach)
> - Paran ka ajiste kantite pwen pa tach POU PWÒP fanmi li nan yon paramèt (yon chif senp, pa yon sistèm konplèks) — si paran pa chanje anyen, 10 pwen default la aplike
> - Sa a ranplase/fikse RPC award_points ki pa egziste jodi a
> BADJ — 2 kalite, tou de fiks (template global, pa pèsonalizab pou kounye a)
> 1. Badj pa kantite pwen total (otomatik, kalkile chak fwa pwen ajoute): 50 pwen = "Kòmanse", 150 = "Bronze", 400 = "Silver", 1000 = "Gold"
> 2. Badj pa aksyon espesifik (kòmanse ak 1 sèlman): "Fidèl" — 7 jou check-in konsekitif
> AFICHAJ
> - Montre kantite pwen total ak lis badj yo genyen sou paj Student nan Admin Dashboard (AdminStudents.tsx)
> - Montre menm enfòmasyon an (pwen + badj) sou pwòp dashboard elèv la (li sèlman, jan RLS deja etabli pou wòl student)
> REGLE:
> - Montre m plan schema/RLS konplè anvan w aplike anyen (nouvo tab/kolòn pou badj si nesesè, trigger pou kalkile badj otomatikman)
> - San danje, revèsib, additive sèlman
> - Verifye bouton "Done" la (blòk tach) MACHE KONPLÈTMAN apre fix la — konfime toude nivo bug la korije (estati "Done" vs "done", AK RPC award_points)
> - Tès bout-an-bout reyèl: konplete yon tach, konfime pwen ajoute, konfime badj bay lè kondisyon ranpli, konfime paran ka ajiste valè pwen pa tach
> - Validation lint/tsc/build, commit, push, deploy, konfime Vercel READY

**Diagnosis:** the Done button is broken on 4 levels, confirmed in prod edge logs:
1. `useDailyBlocks` queries `plan_date`/`block_order`, which don't exist, so it always returns 400 and no blocks render.
2. The code writes `"Done"`/`"In Progress"`, but the CHECK allows only `planned|started|done`.
3. The update writes nonexistent `self_rating`/`time4learning_score`/`notes` and never checks the error.
4. The `award_points` RPC is missing (7 callers).

Other mismatches found: `usePointsHistory` orders by `created_at` (the column is `awarded_at`); `useAchievements` expects `name`/`type`/`criteria_met_at` (the DB has `badge_type`/`milestone`/`earned_at`); the `point_settings` table doesn't exist. `daily_plan`, `reward_points`, `achievements` and `check_ins` all had 0 rows.

**Approved plan (user said yes 2026-09-24):**
- `reward_points` + `source text`, `reference_id uuid`; unique `(student_id, source, reference_id)` where `reference_id` is not null.
- `parent_settings` + `points_per_task int NOT NULL DEFAULT 10 CHECK 0..1000`. The existing policy `auth.uid()=id` means the parent only; no row means 10.
- Trigger AFTER UPDATE OF status ON `daily_plan`: when status becomes `'done'` from something else, insert `COALESCE(points_per_task,10)` with `source='task_done'`, `ref=`task id, `ON CONFLICT DO NOTHING`. SECURITY DEFINER.
- `achievements`: unique `(student_id, badge_type)`.
- Trigger AFTER INSERT ON `reward_points`: from lifetime earned (sum of points > 0), award `points_50`/`points_150`/`points_400`/`points_1000` (milestone = threshold).
- Trigger AFTER INSERT ON `check_ins`: 7 consecutive calendar days ending today (America/New_York) awards `checkin_streak_7` (Fidèl). Badges are permanent.
- `award_points(_student_id,_points,_reason,_source,_reference_id)`: SECURITY DEFINER, family only (`can_impersonate_student`), students and admins 42501, points between -1000 and 1000. Tasks don't use it.
- `get_student_rewards_summary()`: admin only; returns `student_id`, `total_points`, `badges[]`.
- Frontend:
  - `useDailyBlocks` uses the real columns and maps status to UI values.
  - `TodayBlocks` and `syncManager` write `started`/`done` plus timestamps only, check errors, and no longer award task points on the client.
  - `useAchievements` and `usePointsHistory` are aligned to the real columns.
  - New student "Points & Badges" card, AdminStudents points/badges columns, and a parent "points per task" setting that replaces the broken `PointSettingsPanel`. i18n in 10 languages.
- Decisions: (1) disable the legacy client bonuses (check-in, streak, perfect day, 5/5, challenge, category) for now, so only tasks give points. (2) Do NOT touch `rewards_catalog`/`RewardsPanel` (same column-mismatch class); note it for later.

## 2026-09-24 — Remove admin "view as student" (UI + backend)
- Summary: Admins can no longer view as any student. View-as stays family-only (parent, Manager of that parent, co-guardian). An account that is admin AND parent keeps view-as for its own children only, through the parent branch.
  - `can_impersonate_student()` no longer has the `has_role(admin)` branch. It gates both the `impersonation_logs` INSERT (the log-first view-as) and `create-student-account`. So admins can no longer enter a student view, and they can no longer create a login for another family's student, which was a second way in.
  - Dropped 5 admin SELECT policies that were added only to feed admin view-as and that no admin page reads: `daily_plan`, `subject_tracks`, `ai_conversations`, `achievements`, `learning_tools`. Kept `check_ins`, `activity_logs`, `reward_points` and `reward_redemptions`, which AdminEngagement, AdminOverview and AdminRewards use.
  - `AdminStudents.tsx`: removed the "View as" column and button (read-only list).
  - `account-merge`: an admin can no longer approve their own merge request. Otherwise they could move another family's students under their own account and get parent-level view-as. The function is **not deployed** in production (404), so this path was never live. The fix is in source only; nothing was deployed.
  - Also checked: `ai-tutor` already denies admin-only accounts (403) and scopes to `parent_id = caller`. No other edge function or RPC gives admin a student view.
- Files touched: `supabase/migrations/20260924150000_remove_admin_view_as.sql` (applied live), `src/pages/admin/AdminStudents.tsx`, `supabase/functions/account-merge/index.ts`, `CLAUDE.md`, `docs/ACTIVITY_LOG.md`.
- Validation:
  - Live HTTP against production, same script run before and after the migration:
    - Admin (test-admin): `rpc can_impersonate_student` true→**false** for both a test student and a real student from another family. `impersonation_logs` start 201→**403** (42501) for both. `create-student-account` → **403** "Forbidden: you cannot manage this student". `subject_tracks` for all students: 4 rows → **0**.
    - Parent (test-parent): own child true / 201 before and after. Another family's student 403 before and after.
  - DB under RLS as `authenticated`, in rolled-back transactions with temporary links:
    - Manager: managed family true, log stamped `manager`; other family false.
    - Co-guardian: linked family true, log stamped `co_guardian`; other family false.
    - Dany (admin+parent): own child true; 2 other families false.
    - test-admin still sees the 3 students in the admin list.
  - Test data cleaned: 10 e2e `impersonation_logs` rows deleted (0 left). 0 manager_parents / co_guardians / invites left.
  - `npx tsc --noEmit` PASS. `npm run build` PASS. `vitest` 96/96 PASS. eslint on the touched files PASS. `npm run lint` shows 4 errors, all in pre-existing uncommitted edits to `checkin-reminder`, `daily-report`, `morning-reminder` and `weekly-badge`. Those files are not in this commit.
- Risks + rollback: restore the admin `OR` branch in `can_impersonate_student()` and recreate the 5 policies from `20260924120000_student_accounts.sql` section 7. Revert the commit for the UI and `account-merge`.
- Blockers/human actions needed:
  - The test guardian password in `.env.test` is stale (login 400). Co-guardian/Manager were verified at the DB level, not over HTTP.
  - `ai-tutor` checks ownership with `parent_id = caller` only. Manager and co-guardian view-as therefore can't use Mr A for the family's students. This was already the case and is not changed here.

## 2026-09-24 — Email DNS confirmed (Resend, independentmindsedu.org)
- Summary: The user confirmed that the Resend domain `independentmindsedu.org` is verified and that a real test send succeeded earlier today in a separate session (that session's details are not in this log). Recorded as CONFIRMED, and the Stripe live-mode gate item "Email DNS corrected" is now checked in CLAUDE.md. This supersedes the older entry below where `RESEND_API_KEY` was found unset (`hasKey: false`) at the time.
- Files touched: `CLAUDE.md`, `docs/ACTIVITY_LOG.md`.
- Validation: Independent read-only corroboration in production. `messages_log` shows the cron emails (`morning_reminder`, `daily_report`, `checkin_reminder`, channel `email`) with status `sent` every day from 2026-09-21 through 2026-09-24 12:00 UTC, and 0 `failed` rows in the last 3 days. These functions only record `sent` when the Resend API accepts the message, so the key and sending domain are working. This proves API acceptance, not inbox delivery; inbox delivery was the user's own test.
- Risks + rollback: Docs only. Uncheck the gate item if email sending regresses.
- Blockers/human actions needed: The Stripe live-mode gate still has one open item, "Final pricing/terms review completed". Per CLAUDE.md, confirm with the user before touching live keys even once all items are checked.

## 2026-09-24 — Student login accounts, working 'student' role, audited "view as student", AdminStudents fix
- Summary: Approved plan (decisions 1–5 as recommended, plus: keep copy-link UX even though email DNS was reported fixed; fix AdminStudents in the same session).
  **Schema/RLS** (migration `20260924120000_student_accounts.sql`, additive only; no existing policy changed or dropped):
  - `students.user_id` (uuid, nullable, unique, FK auth.users ON DELETE SET NULL).
  - `student_account_invites`: RLS on, no policies, so service role only.
  - `impersonation_logs` (actor_id, actor_role, student_id, action start/end, reason, created_at). Append-only for users; the actor, role and time are stamped server-side by a trigger, so they can't be spoofed. INSERT only if `can_impersonate_student()`. SELECT for own rows and admin.
  - Helpers: `get_my_student_id()`, `can_impersonate_student()`, `get_my_effective_subscription()` (a student is covered by its parent's subscription).
  - `handle_new_user()`: a new branch **only** when an unexpired, unconsumed server-side invite matches the email. It creates the profile with `adult_confirmed=false`, role `student`, links the EXISTING students row, and consumes the invite. Every other signup is unchanged.
  - Linking is deliberately NOT driven by user metadata: a client controls metadata on a normal signUp.
  - Student policies (all `student_id = get_my_student_id()`, so never siblings):
    - read own students row / daily_plan / subject_tracks / check_ins / ai_conversations / achievements / reward_points / activity_logs;
    - insert own check_ins; insert/update own ai_conversations;
    - update own students row: trigger `guard_student_self_update` allows ONLY `profile_photo_url`;
    - update own daily_plan: trigger allows only `status` + its `actual_start`/`actual_end` timestamps;
    - storage: own photo folder.
  - Admin read-only SELECT policies on the per-student tables (for admin "view as").

  **Edge functions**:
  - New `create-student-account`: caller must pass `can_impersonate_student`; refuses a student that already has a login and refuses emails already in use (409); creates the invite, then `generateLink('invite')`; verifies the link and returns the set-password link for the parent to copy.
  - `ai-tutor` (v5): reads all roles instead of `.maybeSingle()`; a student account is gated on its parent's subscription and may only use its own students row.

  **Frontend**:
  - AuthContext loads a student account's own row.
  - Central `startImpersonation`/`stopImpersonation`: the log is written FIRST (no log = no student view) and the end is logged. It supports students outside the viewer's own list (Manager families, admin), guarded against the selection auto-repair.
  - DadPanel view-as button goes through it. ManagerDashboard lists each family's students with "View as". AdminStudents gets "View as".
  - `useSubscription` uses the effective-subscription RPC.
  - "Create login" in StudentProfileCard (parent view) and an optional student-email step in AddStudentFullForm; both show the link with a copy button.
  - The student can change their own photo.
  - 16 new i18n keys × 10 languages.

  **AdminStudents fix**: removed the nonexistent `students.updated_at` from the query (it caused a 400 and a silently empty list), surfaced load errors, and added Login and "View as" columns.
- Files touched: `supabase/migrations/20260924120000_student_accounts.sql` (new), `supabase/functions/create-student-account/index.ts` (new), `supabase/functions/ai-tutor/index.ts`, `src/contexts/AuthContext.tsx`, `src/pages/Index.tsx`, `src/components/DadPanel.tsx`, `src/components/ManagerDashboard.tsx`, `src/components/StudentLoginInvite.tsx` (new), `src/components/StudentProfileCard.tsx`, `src/components/AddStudentFullForm.tsx`, `src/hooks/useSubscription.ts`, `src/pages/admin/AdminStudents.tsx`, `src/lib/i18n.tsx`, `CLAUDE.md`, `docs/ACTIVITY_LOG.md`.
- Validation: tsc 0 new errors (468 → 466); `eslint src` clean; build PASS; vitest 96/96.
  - **Live API E2E against production** (test-parent, test-admin, a temporary sibling):
    - Account setup: create login for another family's student 403; create login for own student 200 + link; second login for the same student 409.
    - Password flow: the link redirects to /reset-password with a session (303); the student sets a password (200); **the student logs in with email+password (200)**. The role is `student` only, `adult_confirmed=false`, and it's linked to the EXISTING row.
    - Isolation: the student sees only their own students row, tasks and check-ins; sibling rows return 0; there is no direct read of subscriptions; the effective subscription is the parent's (`active`).
    - Allowed writes: check-in for self 201; own photo 200; mark own task done 200.
    - Blocked writes: check-in for sibling 403; **grade_level / date_of_birth / display_name → 403**; renaming a task 403; the sibling's photo/task → 0 rows touched.
    - Grade and DOB were verified unchanged in the DB.
    - AI Tutor: own studentId → 200 streamed answer; sibling → 403.
    - View-as: parent view-as logged (actor_role=parent); spoofed actor_id overwritten; another family's student 403; the log is not editable; admin view-as logged (actor_role=admin); admin read access works.
    - A Manager (rolled-back simulation with Aristilde): blocked without a family link, allowed with one (actor_role=manager).
    - Metadata hijack signUp: rejected, 0 accounts created, no row linked.
  - **Real-browser E2E** (Playwright, mobile, local build against production):
    - The student account renders the student view, with no sibling data and no parent sidebar.
    - Parent "view as" via the sidebar: the POST to impersonation_logs returns 201 BEFORE the view switches, and the banner shows who is being viewed. "Back" returns to the parent view and the end is logged (201).
  - All test data was removed and verified: 0 e2e auth users/students/tasks/check-ins, 0 invites, 0 impersonation logs, and Test Student is back to its original values.
  - Supabase security advisor: nothing new callable by anon. The INFO "RLS enabled, no policy" on `student_account_invites` is intentional (service-role only).
- Risks + rollback: `git revert` the commit; redeploy ai-tutor v4 from git history. For the DB, drop the new policies, triggers, functions and tables in reverse order, run `ALTER TABLE students DROP COLUMN user_id`, and restore the previous `handle_new_user()` body (the "unchanged" branch only). Existing parent/Manager/admin access is untouched by design.
- Blockers/human actions needed:
  - **COPPA / minors:** Student accounts are created with `adult_confirmed=false` and use an email the parent controls. **adult_confirmed=false + imèl paran kontwole se yon premye kouch pwoteksyon, PA yon konsantman COPPA konplè verifye — mande revizyon ak yon avoka anvan vrè timoun anba 13 lane itilize sistèm lan an mas.**
  - **Pre-existing bugs found, not fixed (outside this task):**
    - Marking a task started/done in the UI still fails for everyone: the code writes `"In Progress"`/`"Done"` while the CHECK allows only `planned/started/done`; it also writes nonexistent `daily_plan` columns (`self_rating`, `time4learning_score`, `notes`); and the `award_points` RPC doesn't exist. The student's DB permission for it is in place and tested.
    - `ai-tutor` history reads/writes `role`/`content`/`created_at` columns that `ai_conversations` doesn't have, so history is never persisted (answers still work).
    - Per decision 2, student writes to `activity_logs`/`achievements` from the UI are refused (read-only).
  - Email DNS: see the 2026-09-24 "Email DNS confirmed" entry (confirmed and checked in CLAUDE.md).

## 2026-09-23 — selectedStudentId is the student uuid everywhere (DadPanel/AuthContext and friends)
- Summary: AuthContext selected students by `students.id` (uuid), but DadPanel, StudentSwitcherDropdown, StudentSelector and Index compared or set the text label `students.student_id` (e.g. "CH1312-94606"). That caused three visible problems:
  - The selected student was never highlighted in the sidebar.
  - The top student card (StudentSwitcherDropdown) was hidden.
  - Picking the student in the menu turned the selection into the text label, so every per-student tab queried uuid FK columns with text and got 400s / empty data.

  Every per-student table (`daily_plan`, `check_ins`, `subject_tracks`, `reward_points`, `reward_redemptions`, `achievements`, `activity_logs`, `ai_conversations`, `learning_tools`) has `student_id uuid → students(id)`, confirmed in the DB. Fix: all selection/compare/key sites now use `s.id`:
  - DadPanel (menu, highlight, "view as student", impersonation log payload)
  - StudentSwitcherDropdown
  - StudentSelector
  - Index "viewing as student" lookup
  - TelegramSettings `students` lookups (`.eq("id", …)`)

  Both add-student flows now select the new student by its uuid. AddStudentQuickCreate now gets the uuid back (`.select("id").single()`), which also fixes its `subject_tracks` insert that sent the text label into a uuid column.

  AuthContext changes:
  - It loads `grade_level` for the card, which was previously "Grade undefined". A missing grade is now hidden.
  - It re-picks the selection when the stored one isn't one of the parent's uuids (deleted student / legacy text label in `im_selected_student`).
  - This uses a functional state update so an async `refreshStudents()` right after adding a student can't clobber the new selection.

  EducatorDashboard keeps its own local selection (educator system is hidden/unwired) and was not touched.
- Files touched: `src/contexts/AuthContext.tsx`, `src/components/DadPanel.tsx`, `src/components/StudentSwitcherDropdown.tsx`, `src/components/StudentSelector.tsx`, `src/pages/Index.tsx`, `src/components/TelegramSettings.tsx`, `src/components/AddStudentFullForm.tsx`, `src/components/AddStudentQuickCreate.tsx`, `CLAUDE.md`, `docs/ACTIVITY_LOG.md`.
- Validation: tsc 0 new errors and 4 fewer (472 → 468; the stale generated types.ts lacks `students.grade_level`, hence one `as unknown` cast); `eslint src` clean; build PASS; vitest 96/96.
  - Real-browser E2E (Playwright, Pixel 7 viewport, real login as test-parent, local builds against production Supabase), BEFORE (HEAD 5573097) vs AFTER, each with fresh storage and with a legacy text label pre-seeded in `im_selected_student`:
    - Before: top card NOT shown, menu highlight false, menu click stores the TEXT LABEL, Tracks tab requests `student_id=eq.TEST-0001` → 400 ×3, track not shown.
    - After (both storage cases): selection = uuid, top card shown ("Test Student"), menu highlight true, menu click keeps the uuid, Tracks tab requests by uuid → 200, the track is displayed.
  - Temporary test track removed; no E2E leftovers.
- Risks + rollback: `git revert` this commit (frontend only, no DB change). Any browser holding a legacy text label in `im_selected_student` is auto-corrected to the uuid on the next load.
- Blockers/human actions needed, found and NOT fixed (separate pre-existing bugs, same "code vs real schema" class):
  - (1) **Admin → Students tab is empty**: `AdminStudents.tsx` selects `students.updated_at`, which doesn't exist → PostgREST 400 (42703); the component ignores `error` and renders "No students found". The same query without `updated_at` returns all 3 students as admin (RLS is fine).
  - (2) `students.monitoring_enabled` (TelegramSettings hourly-monitor toggle) doesn't exist.
  - (3) Tables `impersonation_logs` and `currency_settings` don't exist (impersonation logging and currency settings silently no-op).
  - (4) `src/integrations/supabase/types.ts` is stale vs the live schema; regenerating it would surface this whole class at compile time.

## 2026-09-23 — Fix multi-role accounts defaulting to "student" (AuthContext)
- Summary: Reported bug: Dany AUGUSTIN's account (`parent` + `admin`) showed the student view on mobile Brave. Root cause is in `AuthContext.tsx`: it read `user_roles` with `.maybeSingle()`, which errors (PGRST116) on more than one row. That error was never checked, so `roleRow` was null and `profile.role` fell back to `"student"`. `useRoleSwitcher` then saw `["student","parent"]` and, with no saved `im_active_role` in localStorage, defaulted to the first entry, `"student"`. Desktop looked fine only because the role had been switched there once and saved. Nothing about this is mobile- or Brave-specific (Brave Shields don't block first-party localStorage); a fresh/private/cleared browser storage just exposes it. Dates from 2026-08-30 (`0256878`), not from this week's changes. It affected every multi-role account: Dany, Aristilde Deslande (`manager` + `parent`), test-admin.
  Fix: AuthContext now reads ALL `user_roles` rows and picks the primary role with new `resolvePrimaryRole()` (`src/lib/profile.ts`), a pure, unit-tested helper:
  - `parent` first, because the app keys the parent experience off `profile.role === "parent"` (student list fetch in AuthContext, 5 `isParent` checks).
  - Then `manager` → `educator` → `admin` in a fixed order, since rows come back unordered.
  - Then any other non-student role.
  - `"student"` only when that is genuinely all the account has.

  Single-role accounts resolve exactly as before. A failed roles read now retries like a failed profile read instead of being taken as "no roles". The saved active role in localStorage still takes precedence in `useRoleSwitcher` (unchanged).
- Files touched: `src/contexts/AuthContext.tsx`, `src/lib/profile.ts`, `src/lib/profile.test.ts`, `CLAUDE.md`, `docs/ACTIVITY_LOG.md`.
- Validation: tsc 0 new errors (472 pre-existing, identical set); `eslint src` clean; build PASS; vitest 96/96 (4 new `resolvePrimaryRole` tests).
  - Real-browser E2E (Playwright, fresh context = empty localStorage, Pixel 7 mobile viewport, real login, reading Index's `data-role`):
    - Production **before** the fix: test-admin (`admin,parent`, Dany's shape) → `student` (bug reproduced); test-parent → `parent`.
    - Local build of the fix against production Supabase:
      - test-admin → `parent`.
      - test-parent temporarily `manager,parent` (Aristilde's shape) → `parent`.
      - test-parent temporarily `student` only → `student`, so no false positive.
      - test-parent `parent` → `parent`.
  - test-parent's roles were restored to `[parent]`, and all accounts' roles were verified identical to before.
  - Dany/Aristilde were not logged into directly (no credentials); their exact role shapes were covered by the cases above.
- Risks + rollback: `git revert` this commit (frontend-only; no DB change). Behaviour change for multi-role users with no saved choice: they now land on their real role instead of the student view.
- Blockers/human actions needed: Users whose browser already saved `im_active_role="student"` while multi-role will no longer be matched to it (student is no longer in their detected roles), so they fall back to parent automatically. No action needed.

## 2026-09-23 — Revoke anon/PUBLIC EXECUTE on get_managed_parent_ids
- Summary: `get_managed_parent_ids(_uid)` is SECURITY DEFINER and trusts its `_uid` argument. Before this change it was executable by PUBLIC and anon, so a signed-out caller could POST `/rest/v1/rpc/get_managed_parent_ids` with any user's id and get the real parent ids linked to them via manager_parents/co_guardians. The exposure was 0 in production at the time (0 links existed), but it was confirmed in a rolled-back simulation. Migration `20260923120000_revoke_anon_get_managed_parent_ids.sql`: `REVOKE EXECUTE … FROM PUBLIC, anon`, while authenticated/service_role keep it, because the 13 "parent manages X" RLS policies call it as `get_managed_parent_ids(auth.uid())` under the caller's role.
- Files touched: `supabase/migrations/20260923120000_revoke_anon_get_managed_parent_ids.sql` (new), `CLAUDE.md`, `docs/ACTIVITY_LOG.md`.
- Validation: tsc 0 new errors (472 pre-existing); `npm run lint` over `src` clean; build PASS; vitest 92/92. The Supabase advisor no longer reports the anon SECURITY DEFINER finding.
  - Live HTTP, anon: the RPC went from 200 + ids to **401 `permission denied for function get_managed_parent_ids`**. All 13 RLS tables now return 401 to anon; `inbox_messages`/`rewards_catalog`/`schedule_templates` changed from `[]` to 401, and no public page queries them (checked routes, AuthContext and syncManager).
  - Live HTTP, RLS with real accounts: a parent sees their own students and subject_tracks; a manager sees the managed family's student and the dashboard RPC shows student_count=1; admin still reads all.
  - Same family through a co-guardian link only: the student is visible.
  - With no link, isolation holds (0 rows).
  - All test data was cleaned up (family auth user, student, links, temporary manager role); Aristilde Deslande is still `[manager, parent]`.
- Risks + rollback: `GRANT EXECUTE ON FUNCTION public.get_managed_parent_ids(uuid) TO PUBLIC, anon;`. Behaviour change: anon queries on the 13 tables now error (401) instead of returning `[]`.
- Blockers/human actions needed:
  - **Remaining gap, not fixed:** a *signed-in* user can still call the RPC directly with another user's `_uid` and receive linked parent ids (ids only; RLS still blocks the data itself). It cannot be closed by revoking from authenticated, since RLS needs it. It needs the function to ignore `_uid` unless it equals `auth.uid()` (or a separate RLS-only helper).
  - `npm run lint` currently fails on 4 errors, all in the uncommitted, user-owned reminder functions (checkin-reminder/daily-report/morning-reminder/weekly-badge: `result.ok ? sent++ : failed++`). They are not in any commit and were left untouched on purpose.

## 2026-09-22 — Task 6/6: Manager dashboard
- Summary: New `ManagerDashboard.tsx`, rendered by Index.tsx when the active role is `manager`. `useRoleSwitcher` now detects the `manager` user_roles row and RoleSwitcher has a Manager entry, so Aristilde Deslande (`[manager, parent]`) gets a Parent/Manager switcher. The families list comes from new `get_my_managed_families()` (SECURITY DEFINER, filtered on `manager_id = auth.uid()`, the same `manager_parents` link the families' RLS uses). It returns only parent_id/display_name/email/student_count/added_at and never the full profile (e.g. `telegram_chat_id`); a row-level policy on `profiles` was rejected for that reason. An "Add family" form calls `manager-create-parent` and shows the returned invite link with a copy button. No other UI called that function before. All new strings are in 10 languages (`manager.*`, `role.manager`).
- Files touched: `src/components/ManagerDashboard.tsx` (new), `src/pages/Index.tsx`, `src/hooks/useRoleSwitcher.ts`, `src/components/RoleSwitcher.tsx`, `src/lib/i18n.tsx`, `supabase/migrations/20260922190000_rename_monitor_to_manager.sql` (function `get_my_managed_families`). Commit `0b63dc6`.
- Validation: tsc 0 new errors (472 pre-existing, identical set); eslint PASS; build PASS; vitest 92/92. Live HTTP E2E on production Supabase:
  - anon gets 401 on the RPC.
  - A non-manager sees 0 families and gets 403 when creating one.
  - A manager creates a family (200 + invite link) and the RPC lists it (name, 0 students, date, email, no extra profile fields).
  - Another user does not see it.
  - After a student is added, student_count becomes 1 and the manager reads that student through RLS.
  - All test data was cleaned up. Vercel READY.
- Risks + rollback: `git revert 0b63dc6` (frontend); `DROP FUNCTION public.get_my_managed_families();`.
- Blockers/human actions needed: Stripe billing for Managers (automatic payment, family limits per plan) is explicitly out of scope. `manager-create-parent` inserts the marker subscription; if a subscriptions row already exists it only logs the error, which is non-fatal.

## 2026-09-22 — Task 5/6: Hide Educator button in Parent Dashboard
- Summary: The Educator system isn't wired to the database, so the "Educators" tab in the Parent Dashboard (`DadPanel`) errored when a parent used it. It is now hidden behind `EDUCATORS_TAB_ENABLED = false`:
  - The nav item is filtered out.
  - The `EducatorsPanel` render is guarded.
  - `initialTab` / `beta-navigate-tab` events can no longer land on the hidden tab.

  No Educator code was removed. EducatorsPanel, EducatorDashboard and the educator role switcher are untouched.
- Files touched: `src/components/DadPanel.tsx`. Commit `0b63dc6`.
- Validation: tsc 0 new errors; eslint PASS; build PASS; vitest 92/92. Not covered by HTTP E2E, because this is a UI-only visibility change; verified through code + build.
- Risks + rollback: Set `EDUCATORS_TAB_ENABLED = true` in `DadPanel.tsx` (or `git revert 0b63dc6`).
- Blockers/human actions needed: Wire the Educator system to the database before re-enabling.

## 2026-09-22 — Task 4/6: Rename Monitor → Manager platform-wide
- Summary: Migration `20260922190000_rename_monitor_to_manager.sql` uses only RENAME/UPDATE (no DROP+CREATE), so no data is lost:
  - `monitor_parents` → `manager_parents` (`monitor_id` → `manager_id`)
  - `monitor_requests` → `manager_requests`
  - `subscriptions.covered_by_monitor_id` → `covered_by_manager_id`
  - Every related constraint and policy was renamed.
  - The `user_roles` CHECK was swapped from `'monitor'` to `'manager'`, with an in-place `UPDATE`.

  Critical catch: the RLS helper `get_managed_parent_ids()` sits behind 13 "parent manages X" policies and names the table in its SQL body, which a rename does not update. It was recreated in the same transaction; without that, every parent would have lost access to their data.

  Edge functions were renamed via `git mv`: `monitor-create-parent` → `manager-create-parent` and `review-monitor-request` → `review-manager-request`. Their contents were updated too, including the metadata key `created_by_monitor` → `created_by_manager`.

  Frontend: Billing.tsx now has "Request Manager Access", AdminUsers.tsx has "Manager Access Requests", and the i18n keys moved from `monitorRequest.*` to `managerRequest.*` (HT: "Mande Aksè Manager").

  Deliberately NOT renamed, because they are unrelated to the role: hourly-monitor, dns-monitor, dns_monitor_history, `monitoring_enabled`, the lucide `Monitor` icon, and the "help monitor a student" copy. Historical migrations were left untouched.
- Files touched: `supabase/migrations/20260922190000_rename_monitor_to_manager.sql` (new), `supabase/functions/manager-create-parent/index.ts` (renamed), `supabase/functions/review-manager-request/index.ts` (renamed), `src/pages/Billing.tsx`, `src/pages/admin/AdminUsers.tsx`, `src/lib/i18n.tsx`. Commit `0b63dc6`.
- Validation: tsc 0 new errors; eslint PASS; build PASS; vitest 92/92.
  - DB after the rename: Aristilde Deslande = `[manager, parent]`, subscription untouched (checked before and after the tests); 0 "monitor" objects left; the helper references `manager_parents`.
  - Live HTTP E2E: parent RLS still works through the recreated helper; the old function names return 410.
  - Full request flow: submit (201) → self-approve blocked (403) → admin approve (200) → re-approve blocked (409) → `manager` role granted.
- Risks + rollback:
  - Reverse the RENAMEs.
  - Run `UPDATE user_roles SET role='monitor' WHERE role='manager'`.
  - Restore the old CHECK and the old `get_managed_parent_ids()` body (from 20260922150000).
  - `git revert 0b63dc6`.
  - Redeploy the old functions from git history.
- Blockers/human actions needed:
  - The old remote functions could not be deleted because the Supabase CLI is not logged in. They were overwritten with 410 "Gone" stubs (not in the repo). After `supabase login`, run `supabase functions delete monitor-create-parent` and `supabase functions delete review-monitor-request`.
  - For about 1–2 minutes between the migration and Vercel READY, the old frontend's monitor_requests queries errored. This was non-fatal; 0 rows existed.
  - Pre-existing, not introduced here: `get_managed_parent_ids(_uid)` is anon-executable with any uuid and leaks linked parent ids. A REVOKE should be done in a future session.

## 2026-09-22 — Task 3/6: Super Pro plan + admin plan editor
- Summary:
  - **Plan key:** `super_pro` added as a plan_key. No CHECK constraint existed, so no schema change was needed.
  - **Access gates:** all of them (ai-tutor, weekly-report-data, SubscriptionGate) check `status`, not `plan_key`, so super_pro automatically gets everything pro has.
  - **App code:** `planIncludes()` in `src/config/plans.ts` ranks super_pro above pro, with an unknown key treated as basic. The Billing page shows "Super Pro". It is not sold on Pricing.
  - **Stripe protection:** trigger `protect_super_pro_plan` stops stripe-webhook upserts from reverting super_pro. Dany has an active Stripe `plus` sub, which would have reverted it at the next `invoice.paid`. Only `admin_set_plan_key()` can change a super_pro.
  - **Admin editor:** AdminBilling has a plan dropdown (basic/plus/pro/super_pro) plus a Save button on each row. Save calls `admin_set_plan_key()` (SECURITY DEFINER, admin-only, updates plan_key only, fixed list). Admins had no UPDATE policy on subscriptions, and a broad one was rejected.
  - **Dany:** `ae29fe11…` set to `super_pro`.
- Files touched: `supabase/migrations/20260922180000_duplicate_check_and_super_pro.sql`, `src/config/plans.ts`, `src/config/plans.test.ts` (new), `src/pages/Billing.tsx`, `src/pages/admin/AdminBilling.tsx`. Commit `ecc1d50`. Local working tree only (uncommitted, pre-existing user changes): `super_pro` added to `shouldOfferAltChannel` in checkin-reminder/daily-report/morning-reminder/weekly-badge.
- Validation: tsc 0 new errors; eslint PASS; build PASS; vitest 92/92 (5 new plan tests).
  - SQL role simulation (rolled back): a Stripe-style upsert keeps super_pro; non-admin forbidden; invalid key rejected.
  - Live HTTP: non-admin 403; invalid plan 400; admin sets super_pro 204; Dany confirmed `super_pro / active`.
  - Vercel READY.
- Risks + rollback: `git revert ecc1d50`; `DROP TRIGGER protect_super_pro_plan ON subscriptions; DROP FUNCTION protect_super_pro_plan(), admin_set_plan_key(uuid,text);`; `UPDATE subscriptions SET plan_key='plus' WHERE user_id='ae29fe11-98f4-4c72-9ce5-4e3a2c53c122';`.
- Blockers/human actions needed: basic/plus/pro set by an admin on a Stripe-billed user will still be overwritten by the next Stripe event; only super_pro is protected.

## 2026-09-22 — Task 2/6: Global duplicate-student warning
- Summary: RLS limits each parent to their own family, so a client SELECT cannot see duplicates in other families. New `student_duplicate_exists(name, dob)`:
  - SECURITY DEFINER; searches ALL families but returns only a boolean, never ids or another family's data.
  - Authenticated only (anon revoked).
  - Name comparison ignores case and extra whitespace.
  - Skipped when DOB is empty, since a name alone is too common platform-wide.

  In AddStudentFullForm, a match shows an AlertDialog: "A student with this name/DOB seems to already exist", with "Continue anyway" or "Cancel". It is a warning, not a block. New strings are in 10 languages.
- Files touched: `supabase/migrations/20260922180000_duplicate_check_and_super_pro.sql`, `src/components/AddStudentFullForm.tsx`, `src/lib/i18n.tsx`. Commit `ecc1d50`.
- Validation: tsc 0 new errors; eslint PASS; build PASS; vitest 92/92.
  - SQL role simulation: another family gets `true`, while RLS still returns 0 rows; wrong DOB or null DOB returns false; anon gets permission denied.
  - Live HTTP: anon 401; another family's parent gets `true`; RLS still returns 0 rows.
- Risks + rollback: `DROP FUNCTION public.student_duplicate_exists(text, date);` + `git revert ecc1d50`.
- Blockers/human actions needed: A signed-in user who already knows a child's exact name AND DOB can confirm the child exists on the platform. That is the only information revealed. Accepted trade-off.

## 2026-09-22 — Task 1/6: Remove duplicate student
- Summary: Deleted the duplicate "Christian Dany AUGUSTIN" `0583302b-73e9-49a2-998e-43eca2fda9dc` (created 16:14:42) and kept `71140924-fa81-4bbe-aac0-5fbf7faca9c1` (15:33:38). Before deleting, all 9 tables with an FK to `students` were checked. Only `subject_tracks` had rows (4), and those were deleted first, in one transaction. A JSON backup of all 5 rows was kept locally.
- Files touched: None (data only, production DB).
- Validation: After deletion, a single "Christian Dany AUGUSTIN" row remains (`71140924…`); 0 orphaned rows.
- Risks + rollback: Re-insert the student + 4 subject_tracks from the JSON backup (session scratchpad `backup_duplicate_student_0583302b.json`).
- Blockers/human actions needed: The kept student has **0 subject_tracks**; the duplicate held the 4 default tracks. Re-create them if needed.

## 2026-09-22 (4th follow-up) — Grant Aristilde Deslande Monitor access + "Request Monitor Access" feature
- Summary: Two explicit tasks, executed as one continuous flow (no mid-task confirmation, per instruction).
  **Task 1 — Aristilde Deslande:** Found the account via `profiles.display_name ILIKE '%Aristilde%'` (single match, `b9a838b1-8d17-48c4-a0e7-025b0e06ca60`, existing role `parent`). Granted `'monitor'` additively (`INSERT ... ON CONFLICT DO NOTHING` on `user_roles` — roles now `[parent, monitor]`). Gave them a marker-only `subscriptions` row (`status:'active'`, `plan_key:'pro'`, no `stripe_customer_id`/`stripe_subscription_id`, `covered_by_monitor_id` left null since this is their OWN access, not a monitor-covered parent) via `upsert` on `user_id` — same no-Stripe-fields pattern as `covered_by_monitor_id` rows from the previous session, so their own AI Tutor/Weekly Reports access is never gated. Verified via direct SQL only (no login attempted — this is a real person's account, not a test fixture, so it wasn't logged into) that the role and subscription state are exactly as intended and untouched by the later cleanup for Task 2's tests.
  **Task 2 — Request Monitor Access:**
  - New `monitor_requests` table (`user_id, organization_name, reason, expected_families_count, status CHECK IN ('pending','approved','rejected'), reviewed_by, reviewed_at, created_at`). RLS: user can INSERT/SELECT only their own row (no UPDATE/DELETE policy for the requester at all — a submitted request can't be self-edited or self-approved), admin gets ALL (`has_role admin`, matching the `beta_requests` convention already in this codebase), service_role gets ALL.
  - New edge function `review-monitor-request`: admin-only (checked via `user_roles` directly with the service client, not just RLS), takes `{request_id, decision}`, refuses to re-process a non-`pending` request (`409`), updates `monitor_requests` (`status`, `reviewed_by`, `reviewed_at`), and on `approved` additionally `upsert`s the `'monitor'` role onto `user_roles` (`ignoreDuplicates: true` — additive, "menm mekanis ak asiyasyon manyèl la", never replaces existing roles). On `rejected`, `user_roles` is untouched.
  - Frontend request form added to `Billing.tsx` (the closest existing account-level settings page — no dedicated Settings/Profile page exists in this app) rather than a new route, since it's a natural sibling to "manage your plan." Shows the form only when the user isn't already a monitor and has no pending request; shows a rejected-notice banner (can resubmit) or a pending/already-monitor state otherwise. Added 11 new i18n keys (`monitorRequest.*`) with real EN/HT/FR translations and EN-placeholder ES/PT/AR/ZH/DE/JA/RU — matching this file's own established pattern (verified by inspection: existing `billing.*` keys already follow the same EN/HT/FR-real, rest-placeholder convention, not full translations in all 10).
  - Admin review UI added to `AdminUsers.tsx` as a new "Monitor Access Requests" table (same visual pattern as the existing "Merge Requests" section), Approve/Reject buttons that call the edge function (not a direct table update, since approval must also touch `user_roles` atomically) — plain English text throughout, matching this file's own existing convention (the entire file has no `useI18n()` usage at all; admin surfaces in this codebase are English-only by established precedent, not i18n'd).
  **Live end-to-end testing (real accounts, real RLS, no simulation):** `test-parent` submitted a request via REST with their own JWT (200). Confirmed `test-parent` cannot self-approve by `PATCH`ing the row directly (RLS silently filters it out, `200` with an empty array — no matching row to update) and cannot call `review-monitor-request` themselves (`403 Forbidden`). `test-admin` approved it via the real edge function (`200`) — verified via SQL that `status`/`reviewed_by`/`reviewed_at` were set correctly AND `user_roles` gained `'monitor'` additively (`[parent, monitor]`). Confirmed re-processing the same (now-approved) request is rejected (`409`). Submitted and rejected a second request via the same flow (`200`, `status:'rejected'`, `user_roles` untouched). Cleaned up: removed the test `'monitor'` role grant from `test-parent` and both test `monitor_requests` rows; verified via SQL afterward that `test-parent` is back to `[parent]` only and 0 requests remain. Re-verified Aristilde Deslande's grant was untouched by any of this cleanup.
- Files touched: `supabase/migrations/20260922170000_monitor_requests.sql` (new), `supabase/functions/review-monitor-request/index.ts` (new), `src/pages/Billing.tsx` (request form), `src/pages/admin/AdminUsers.tsx` (review section), `src/lib/i18n.tsx` (11 new keys), `CLAUDE.md`, `docs/ACTIVITY_LOG.md`. Remote: migration applied to `gyvjcwuwfwrwwwnuwlex`; `review-monitor-request` deployed (v1); `user_roles`/`subscriptions` rows updated directly for Aristilde Deslande (`b9a838b1-...`).
- Validation: `npx eslint`/`npx tsc --noEmit`/`npm run build` all clean on every touched file. Every access path above live-tested with real JWTs and real RLS/edge-function enforcement, not simulated.
- Risks + rollback: Purely additive (new table, new edge function, new UI sections, new column values on 2 existing rows) — no existing table, policy, or column altered. Rollback: `git revert` the code commit; for the DB side, `DROP TABLE monitor_requests` (nothing else references it) and, if ever needed, revoke the two grants with a plain `DELETE`/`UPDATE` against `user_roles`/`subscriptions` for Aristilde Deslande's account.
- Blockers/human actions needed: None. Aristilde Deslande's Monitor role and unlimited access are live now; the Request-Monitor-Access flow is live end-to-end (form → admin review → automatic role grant).

## 2026-09-22 (3rd follow-up) — Monitor role + co-guardian RLS + 2(+2) uuid/text bugs
- Summary: User requested a full plan review before any code (per their explicit rule); plan below was approved with 2 amendments (add "parent reads own monitor link"; defer the co-guardian invite mechanism itself).
  **Diagnosis before planning:** read every `pg_policies` row, `has_role()`'s source, `user_roles`' CHECK constraint, `co_guardians`/`guardian_invites`/`subscriptions` real schemas, and both co-guardian edge functions (`send-guardian-invite`, `accept-guardian-invite`). Found the co-guardian invite mechanism is broken independently of RLS: those 2 edge functions and `CoGuardiansPanel.tsx` all consistently expect a per-STUDENT schema (`guardian_invites.student_id/invitee_email/permissions(jsonb)/token_hash`, `co_guardians.student_id`) that doesn't exist — the live tables are per-PARENT (`parent_id`/`guardian_id`, plaintext `token`, flat permission columns), from yet another never-applied old migration. Flagged this to the user before planning further; they confirmed deferring the invite-mechanism fix (2 edge functions + `CoGuardiansPanel.tsx` UI) to a future session, scoping this session to making backend *access* (RLS) correct for whatever `co_guardians` rows exist however they got there.
  **Approved plan, applied as designed:**
  - `user_roles_role_check` CHECK extended to include `'monitor'`. Assignment stays admin-only (only INSERT policy on `user_roles` is `has_role admin`, untouched).
  - New `monitor_parents(monitor_id, parent_id)` table — source of truth for monitor↔parent coverage. RLS: monitor reads own, parent reads own (link visibility, the one addition beyond the user's original spec, approved), admin ALL, service_role ALL. No client-side INSERT path — only the new edge function or an admin can create a row.
  - New `get_managed_parent_ids(_uid)` SECURITY DEFINER SQL function (same pattern as `has_role()`) returning `{self} ∪ {parents this uid monitors} ∪ {parents this uid co-guards}` — one shared definition instead of duplicating 2 extra subqueries across 13 policies.
  - Rewrote all 13 existing "parent manages X" policies (`students`, `rewards_catalog`, `schedule_templates`, `inbox_messages`, `achievements`[SELECT-only, unchanged], `activity_logs`, `ai_conversations`, `check_ins`, `daily_plan`, `learning_tools`, `reward_points`[SELECT-only, unchanged], `reward_redemptions`, `subject_tracks`) to route through `get_managed_parent_ids()` instead of a bare `parent_id = auth.uid()`/`students.parent_id = auth.uid()` subquery — same access level each already had, monitor/co-guardian never get more than the parent did. Deliberately untouched: `co_guardians`, `guardian_invites`, `parent_settings`, `subscriptions` (access-control/billing stay parent-exclusive).
  - `subscriptions`: added nullable `covered_by_monitor_id uuid references auth.users`, and relaxed `stripe_customer_id`/`stripe_subscription_id`/`plan_key` to nullable so a monitor-created parent (no real Stripe subscription yet) can have a row. `status` keeps its existing CHECK unchanged — reused the already-allowed `'incomplete'` value (already reads as "not active" in `useSubscription.ts` and every server-side gate) rather than inventing a new status. Confirmed `stripe-webhook`'s `upsert(..., {onConflict:"user_id"})` only ever sets the columns it lists, so a later real Stripe checkout fills the Stripe fields without touching `covered_by_monitor_id`. Zero Stripe/webhook code changed.
  - New edge function `monitor-create-parent`: verifies caller has role `monitor` (or `admin`), then creates the parent account via `admin.auth.admin.generateLink({type:'invite', ...})` — **not** `inviteUserByEmail`, switched after first testing: `inviteUserByEmail` rejected every test address (`delivered@resend.dev`, even a `*.independentminds.test` address matching the working fixture accounts' own domain) with a generic "invalid" error, consistent with this project's known-unresolved email/DNS gate item — `generateLink` creates the same user via the same `handle_new_user()` trigger path but returns a usable `action_link` without depending on the mailer, same resilience pattern `send-guardian-invite` already uses (always return a link, email delivery is best-effort). Inserts the marker `subscriptions` row (`status:'incomplete'`, `covered_by_monitor_id`) and the `monitor_parents` link row.
  - Fixed the 2 requested bugs: `AddStudentFullForm.tsx` now captures `.select("id").single()` on the students insert and uses that uuid for `subject_tracks`/`daily_plan`, instead of the text label; `StudentProfileCard.tsx`'s 3 `.eq("student_id", studentId)` calls (select, profile-save update, photo-update) now query by `.eq("id", studentId)` since every caller passes the uuid.
  - **2 more bugs of the same category, found live-testing the first fix (fixed in the same pass, same file already open):** `daily_plan`'s real column is `planned_date` (not `plan_date`), and it has no `start_time`/`end_time`/`notes`/`block_order` columns at all — folded those into `title` (e.g. "Reading (08:00-09:00) - practice") rather than dropping the data or leaving broken column names, flagging that per-block time ranges aren't really modeled and would need a schema decision later. Also `daily_plan_status_check` only allows lowercase `planned`/`started`/`done` — the code used `"Planned"`; fixed to `"planned"`.
  - **Also fixed in `StudentProfileCard.tsx` while there:** its photo-upload path used the uuid `studentId` prop as the storage folder name, but the `student-photos` RLS policy (from the previous session's migration) scopes access by the student's TEXT label — matching `AddStudentFullForm.tsx`'s upload convention (uploads happen before the uuid exists). Switched to `student?.student_id` (the text label from the already-fetched row) so uploads from the profile card actually pass RLS instead of getting silently rejected.
  **Live end-to-end testing** (all via real accounts, no mocking): temporarily granted `test-admin` a `monitor` role. Called `monitor-create-parent` → verified `profiles`/`user_roles('parent')`/`subscriptions('incomplete', covered_by_monitor_id set)`/`monitor_parents` all correct via direct SQL. Set a password on the new test parent via SQL (`crypt()`, same technique used to provision the original fixture accounts) since `generateLink` doesn't itself issue a usable password. As **monitor**: `GET`/`INSERT` on `students`/`daily_plan` for the managed family → both passed; `GET` on `co_guardians` for that family → correctly empty (blocked). As monitor: `INSERT` into `co_guardians` → correctly `403` (RLS violation) — monitor cannot touch co-guardians. As the **new parent** (independent of the monitor that created them): `INSERT` then `DELETE` their own `co_guardians` row → both succeeded, confirming the guarantee. As **`test-parent`** made a co-guardian of the new parent's family (via a direct SQL insert into `co_guardians` — the invite mechanism itself being deferred): `GET students` and `PATCH daily_plan` for that family both succeeded — read+write confirmed at the parent's own level. Separately, simulated the fixed `AddStudentFullForm.tsx`/`StudentProfileCard.tsx` flows via REST with `test-parent`'s real JWT (insert-then-uuid-chain for students/subject_tracks/daily_plan; `.eq("id",...)` select/update) — all succeeded. All test data (the temporary monitor role, the new test parent and everything it cascaded, the uuid-fix test student, a test mutation on the real fixture student) cleaned up and verified gone via `execute_sql` counts afterward.
- Files touched: `supabase/migrations/20260922150000_monitor_role_and_coguardian_rls.sql` (new), `supabase/functions/monitor-create-parent/index.ts` (new), `src/components/AddStudentFullForm.tsx` (uuid + planned_date/status/title fixes), `src/components/StudentProfileCard.tsx` (id-based queries + photo path fix), `CLAUDE.md`, `docs/ACTIVITY_LOG.md`. Remote: migration applied to `gyvjcwuwfwrwwwnuwlex`; `monitor-create-parent` deployed (v2, after the generateLink switch).
- Validation: `npx eslint`/`npx tsc --noEmit`/`npm run build` all clean on both touched frontend files. Every access path above live-tested with real JWTs and real RLS enforcement (not `SET ROLE` simulation) — monitor↔family (pass), monitor✗co_guardians (blocked both read and write), parent's independent co-guardian control (pass), co-guardian↔family read+write (pass), both uuid-bug-fix flows (pass).
- Risks + rollback: Schema changes are additive (new table, new column, CHECK constraint widened, 3 NOT NULLs relaxed) or policy rewrites that preserve each table's existing access ceiling — no table lost a capability parents already had. Rollback: `git revert` the code commit; for the DB side, drop `monitor_parents`, restore the 13 policies' previous `USING` clauses, re-narrow the CHECK constraints and NOT NULLs, and drop `get_managed_parent_ids()`/`covered_by_monitor_id` — all reversible, no data loss since nothing existing was altered or deleted.
- Blockers/human actions needed: (1) Co-guardian invite mechanism (2 edge functions + `CoGuardiansPanel.tsx`/`StudentCoGuardiansSection.tsx`) is still fundamentally broken against the real schema — explicitly deferred by the user to a future session; until fixed, the only way a `co_guardians` row gets created is direct DB access, not the UI. (2) No admin UI exists yet to grant the `monitor` role or to browse/create parents as a monitor — this session was scoped to backend/RLS only; a minimal Monitor-facing UI (and an admin role-assignment control) is a natural next step once this backend is confirmed. (3) `daily_plan`'s lack of real time-range columns (start/end time, notes, ordering) means extracted-schedule blocks only carry that info folded into `title` — a real fix needs a schema decision, not attempted here.

## 2026-09-22 (2nd follow-up) — Restore 9 dropped student-profile columns + learning_tools/student-photos
- Summary: User reported a new error on production — `Could not find the 'address' column of 'students' in the schema cache` when adding a student. Diagnosed read-only first (per explicit ask), showed the diagnostic, then implemented the approved fix.
  **Diagnosis:** `information_schema.columns` confirmed the live `students` table has no `address` column (nor 8 others). Traced the writers: `AddStudentFullForm.tsx` (`.insert()`, the "add student" wizard) and `StudentProfileCard.tsx` (`.update()`, the profile edit dialog) both actively read/write `date_of_birth, nationality, address, profile_photo_url, enrollment_date, academic_year, parent_name, parent_email, parent_whatsapp` — 9 columns total, not just `address`. `PrivacyPolicy.tsx:86` already documents collecting this exact data. Cross-referenced `list_migrations` (live) against `supabase/migrations/*.sql` (git): the live project's migration history only starts at `20260823155444` — none of the ~35 older dated migration files (which do define all 9 columns, across `20260305014211` (original CREATE TABLE: `parent_name`/`parent_email`/`parent_whatsapp`) and `20260319222728` (`date_of_birth`/`nationality`/`address`/`profile_photo_url`/`enrollment_date`/`academic_year`)) were ever applied to this project. Same root cause as the `grade_level` fix from 2026-09-07, just 9 columns instead of 1 — confirmed this is case (a) (a real, actively-used, documented feature whose backing columns are simply missing), not dead code.
  Also found while tracing: `20260319222728` additionally creates a `learning_tools` table (used live by `LearningToolsHub.tsx`, part of `DadPanel.tsx`'s tabs) and a `student-photos` storage bucket (used by `AddStudentFullForm.tsx`'s photo upload and `src/lib/studentPhoto.ts`'s signed-URL resolver) — neither exists on the live project either. Surfaced both as directly-related findings in the diagnostic; user asked to include both in the fix, conditioned on the old migration defining them clearly (it does).
  **Fix, approved and applied:** New migration `20260922120000_restore_student_profile_fields.sql`, applied via `apply_migration` (not a CLI deploy step) and verified against `information_schema.columns` afterward (all 9 present):
  - `ALTER TABLE students ADD COLUMN IF NOT EXISTS ...` for all 9 columns, defs mirroring the original two migrations exactly (including `enrollment_date DEFAULT CURRENT_DATE` and `academic_year DEFAULT '2025-2026'`).
  - `learning_tools` recreated, but NOT copied verbatim: the old migration's RLS used `is_my_student()`/`get_my_role()`, neither of which exists on this project (confirmed via `information_schema.routines` — empty), and its `student_id` was the legacy TEXT label. Checked `LearningToolsHub.tsx`'s caller chain (`DadPanel.tsx` → `selectedStudentId`, the same uuid `ai-tutor`/`ActivityFeed`/`daily_plan` already use) and confirmed it passes the modern `students.id` uuid — so `learning_tools.student_id` was defined as `uuid references students(id)`, with RLS matching the current `daily_plan`/`activity_logs`/`subject_tracks` ownership-policy shape (`student_id IN (SELECT id FROM students WHERE parent_id = auth.uid())`) instead of the dead functions.
  - `student-photos` bucket recreated with `public: false`, not `true` like the old migration. `src/lib/studentPhoto.ts`'s own comment says the bucket "is private, so we must request a short-lived signed URL," and it only ever calls `createSignedUrl` — the app code had already moved past the old migration's "Anyone can view student photos" public-read design. Recreating it public would have reintroduced a real privacy problem (world-readable children's photos) that the code no longer has; used a parent-owns-via-folder-name RLS policy on `storage.objects` instead (`(storage.foldername(name))[1] IN (SELECT student_id FROM students WHERE parent_id = auth.uid())`, matching the folder path `AddStudentFullForm.tsx` actually uploads to). This is a deliberate deviation from the old migration's literal definition, flagged to the user rather than done silently.
  - Fixed a related bug found while reading `AddStudentFullForm.tsx` for this task: it collects `nationality` in the form (state, and shows it in the confirmation summary) but never included it in the `students` insert object — added the missing field.
  **Validation:** `npx eslint` (touched file, clean), `npx tsc --noEmit` (full project, clean), `npm run build` (clean). Committed (`ecad982`) and pushed; Vercel production deployment confirmed `READY` (`dpl_ECfeXQgRp5DCVz8YfqLtLLa7vKAu`).
  **Live end-to-end test:** logged in as `test-parent@independentminds.test` for a real JWT, then `POST /rest/v1/students` with that JWT (same request shape `AddStudentFullForm.tsx`'s `supabase.from("students").insert()` produces) — all 9 fields filled (`date_of_birth`, `nationality`, `address`, etc.) — returned `201 Created` with every field saved correctly (`enrollment_date`/`academic_year` defaults applied). Deleted the test row immediately after (`204`), confirmed gone via `execute_sql` count.
  **New findings surfaced by this test (not fixed, flagged for a future session):** (1) `AddStudentFullForm.tsx`'s `subject_tracks` and `daily_plan` inserts (the default-tracks and extracted-schedule steps right after the student insert) still send `studentId.toUpperCase()` — the text label — but both `subject_tracks.student_id` and `daily_plan.student_id` are now `uuid` (confirmed via `information_schema.columns`). These inserts fail (invalid uuid syntax) but are caught non-fatally (`console.error` only, not re-thrown), so student creation itself still succeeds and reports success to the user — but a newly created student silently gets no default subject tracks and no schedule blocks. Same class of bug as the uuid/text mismatches fixed in `ai-tutor`/`ActivityFeed`/`TodayBlocks` earlier this project. (2) `StudentProfileCard.tsx` queries `students` via `.eq("student_id", studentId)` — the text-label column — but every caller (`DadPanel.tsx`) passes the uuid `id` as `studentId`. This looks like the profile view/edit dialog currently returns no rows for real students (not verified live this session, since it wasn't part of the ask — flagged from static reading only).
- Files touched: `supabase/migrations/20260922120000_restore_student_profile_fields.sql` (new), `src/components/AddStudentFullForm.tsx` (nationality field), `CLAUDE.md`, `docs/ACTIVITY_LOG.md`. Remote: `students` table (9 columns added), `learning_tools` table (created), `student-photos` storage bucket (created, private) + 1 `storage.objects` RLS policy — all applied directly via Supabase migration tooling to project `gyvjcwuwfwrwwwnuwlex`.
- Validation: `npx eslint`, `npx tsc --noEmit`, `npm run build` all clean. Live REST insert with a real fixture JWT: `201`, all 9 fields round-tripped correctly. Vercel production deployment confirmed `READY`.
- Risks + rollback: Additive schema changes only (`ADD COLUMN IF NOT EXISTS`, `CREATE TABLE IF NOT EXISTS`, `ON CONFLICT DO NOTHING` on the bucket insert) — safe to re-run, nothing dropped or renamed. Rollback: `git revert ecad982` for the code/migration file (migration file revert doesn't undo the already-applied remote change; would need a follow-up `ALTER TABLE ... DROP COLUMN` migration if the columns must actually come back out, not expected to be needed). RLS on the new `learning_tools` table and `student-photos` policy are ownership-scoped from the start, so no exposure window.
- Blockers/human actions needed: (1) `AddStudentFullForm.tsx`'s `subject_tracks`/`daily_plan` inserts need the same uuid fix as everything else that hit this bug — not done this session (out of scope of what was asked), a newly created student currently gets no default tracks/schedule. (2) `StudentProfileCard.tsx`'s `student_id`-vs-`id` query mismatch, flagged from static reading only, not live-verified — worth confirming and fixing in a future session before relying on the profile edit dialog. (3) Same pre-existing unrelated uncommitted changes from before this session (`.env.test`, the 4 reminder-function rewrites) are still sitting untouched in the working tree.

## 2026-09-22 — AI Tutor provider migration: Lovable gateway/gemini → OpenAI gpt-4.1
- Summary: User made the final provider decision (deferred since the 2026-09-07 Milestone 3 follow-up): connect `ai-tutor` directly to OpenAI using the user's own OpenAI account, model `gpt-4.1` (not `google/gemini-2.5-flash`, not OpenRouter). Reviewed `ai-tutor/index.ts` first and confirmed the endpoint construction (`${AI_GATEWAY_URL}/v1/chat/completions`), request shape (`{model, messages, stream:true}`, `Authorization: Bearer`), and SSE streaming parse (`data: {...}`, `choices[0].delta.content`, terminated by `data: [DONE]`) already match OpenAI's standard Chat Completions API exactly — so the only required code change was the model string. Confirmed with the user that `AI_GATEWAY_URL` must be set **without** a trailing `/v1` (`https://api.openai.com`), since the code appends `/v1/chat/completions` itself.
  While reviewing, found `supabase/functions/extract-schedule/index.ts` (schedule/timetable extraction from uploaded documents) reads the **same** `AI_GATEWAY_URL`/`AI_GATEWAY_API_KEY` secrets and also hardcodes `google/gemini-2.5-flash` — since these secrets are shared across functions, repointing them at OpenAI would silently break `extract-schedule` the moment it's ever invoked (OpenAI has no such model). Flagged to the user before making changes; user confirmed applying the same model fix there. Checked `list_edge_functions`/`get_edge_function` on the live project (`gyvjcwuwfwrwwwnuwlex`) and confirmed `extract-schedule` has never actually been deployed to Supabase (`NotFoundException`) — so this was a source-only fix with no live blast radius; not deployed as part of this task (deploying a previously-never-shipped function is a separate decision, left for whenever that feature is actually launched).
  Applied both model changes (`google/gemini-2.5-flash` → `gpt-4.1`), then: `npx eslint .` (clean on both touched files — 4 pre-existing unrelated errors surfaced in `checkin-reminder`/`daily-report`/`morning-reminder`/`weekly-badge`, from separate uncommitted work already in the working tree before this task started, not touched), `npx tsc --noEmit` (clean, full project), `npm run build` (clean). Committed only the 2 touched files (`129b3cc`) — deliberately did not bundle the pre-existing uncommitted changes to `.env.test`/the 4 reminder functions sitting in the working tree, since they're unrelated to this task and weren't reviewed here (see Blockers below). Pushed to `main`.
  Deployed the updated `ai-tutor` source to Supabase via `deploy_edge_function` (version 3 → 4, `verify_jwt: true` preserved). Live-tested end to end using the `test-parent@independentminds.test` fixture (real login via `/auth/v1/token`, real JWT, active subscription confirmed via `execute_sql`, real owned `students.id`): `POST /functions/v1/ai-tutor` with a real chat message returned `200`, a genuine SSE stream, `"model":"gpt-4.1-2025-04-14"` on every chunk (proof it's actually OpenAI's gpt-4.1, not an error or a stale gateway), and a coherent bilingual EN/HT answer matching the system prompt's format exactly ("7 x 8 equals 56." / "7 fwa 8 egal 56.").
  **New finding surfaced by this test (not fixed, out of scope):** queried `ai_conversations` after the test call to confirm history persistence — the query failed (`column "role" does not exist`). `information_schema.columns` shows the table's real shape is `id, student_id, subject, messages (jsonb), updated_at` — nothing like the `role`/`content`/`created_at` per-row shape `ai-tutor/index.ts` reads and writes (history load, user-message save, assistant-message save, and the 100-row/90-day cleanup queries all target those non-existent columns). Every one of these calls fails silently (errors are either ignored via destructuring `{data}` without checking `error`, or logged via `console.error` only) — so conversation memory/history has never actually worked, but it fails closed: the core chat response itself is unaffected, the student/parent just never gets multi-turn context and no history is retained. This is the same class of stale-schema bug as the `daily_plan`/`messages_log`/`profiles.role` issues fixed earlier this project (schema redesign that several functions/components never caught up to) — flagged for a future session, consistent with how those were handled (fix confirmed bugs only when explicitly scoped in).
  Confirmed the Vercel deployment triggered by the `main` push reached `READY` in production.
- Files touched: `supabase/functions/ai-tutor/index.ts` (model swap only), `supabase/functions/extract-schedule/index.ts` (model swap only, not deployed), `CLAUDE.md` (5th Stripe live-mode gate item checked, Recent updated), `docs/ACTIVITY_LOG.md`. Remote: `ai-tutor` edge function redeployed (v4) on Supabase project `gyvjcwuwfwrwwwnuwlex`; `AI_GATEWAY_URL`/`AI_GATEWAY_API_KEY` secrets updated by the user directly in the Supabase dashboard (not touched by this session — no secret values were read or logged).
- Validation: `npx eslint .`, `npx tsc --noEmit`, `npm run build` all clean for the 2 touched files (pre-existing unrelated lint errors in 4 other already-modified functions left as found, not introduced here). Live end-to-end test against the real deployed function with a real fixture JWT and real OpenAI call: `200`, streamed `gpt-4.1-2025-04-14` response, correct content. Vercel production deployment for the `main` push confirmed `READY`.
- Risks + rollback: Low risk, single-line model-string change per function; `ai-tutor`'s request/response handling code was already OpenAI-shaped so no other logic changed. Rollback: `git revert 129b3cc` and redeploy `ai-tutor` v3 (or re-run `deploy_edge_function` with the prior source, retained in git history), or simply set `AI_GATEWAY_URL`/`AI_GATEWAY_API_KEY` back to the previous Lovable-gateway values (no code change needed for a secrets-only rollback). `extract-schedule` was never live, so no rollback surface there.
- Blockers/human actions needed: (1) `ai_conversations` schema mismatch (above) means AI Tutor conversation history/memory doesn't work — needs a real fix (either migrate the table to match the code, or rewrite the function to use the actual `messages jsonb` column) before that's a real feature, not urgent since chat itself works. (2) `extract-schedule` is fixed in source but still not deployed — deploy it whenever that feature is actually launched. (3) Pre-existing uncommitted working-tree changes untouched by this session, still pending: `.env.test` (test password rotation), and substantial rewrites of `checkin-reminder`/`daily-report`/`morning-reminder`/`weekly-badge` (600+ line diff, looks like a self-contained-imports refactor) — none reviewed or committed here since they're unrelated to this task; flagging so they aren't lost or mistaken for reviewed/ready.

## 2026-09-07 (Milestone 3 follow-up) — Confirm AI Tutor is live pricing copy; trace AI_GATEWAY_* history; add to the Stripe live-mode gate
- Summary: Two questions, both investigated read-only, no code changed.
  (1) **Is AI Tutor referenced in current pricing copy?** Confirmed yes: `src/pages/Pricing.tsx:91` renders `plan.highlights.map(...)` from `src/config/plans.ts`, and every plan (`basic`/`plus`/`pro`) lists `"AI Tutor access"` as a highlight — rendered as a checkmarked bullet. `/pricing` is a top-level route in `App.tsx`, not wrapped in any auth guard — publicly visible to any visitor, not just logged-in users. So yes, it's actively being advertised, not a dormant/unused config value. Weekly progress reports are listed the same way, same tiers.
  (2) **What does `AI_GATEWAY_API_KEY`/`AI_GATEWAY_URL` actually need?** Rather than guess, traced it via `git log --follow` on `supabase/functions/ai-tutor/index.ts`. The function originally (earliest commit `cb9e56e`) used `LOVABLE_API_KEY` and a hardcoded `https://ai.gateway.lovable.dev/v1/chat/completions` — i.e. Lovable's own AI Gateway product. Found the exact commit that changed this: `d84bd37` ("chore: complete final lovable removal and gateway neutral migration", 2026-07-10) — confirmed via full diff that this was **a pure rename** (`LOVABLE_API_KEY`→`AI_GATEWAY_API_KEY`, hardcoded URL→`AI_GATEWAY_URL` env var read). The model string (`google/gemini-2.5-flash`) and endpoint shape (`/v1/chat/completions`, OpenAI-compatible SSE streaming) are unchanged — no new provider was ever actually integrated despite the "gateway neutral" framing. So today the code still only knows how to speak to something Lovable-AI-Gateway-shaped; whoever fills in these two secrets needs to either provision a real Lovable AI Gateway key (from the Lovable dashboard for this project, if still accessible) or migrate the code to a different OpenAI-compatible provider serving the same model ID (e.g. OpenRouter) — the latter not yet built, deferred to next session per the user's explicit choice.
  Given AI Tutor is confirmed live-advertised, added a 5th condition to the standing Stripe live-mode gate in `CLAUDE.md` (previously 4) — not urgent in the sense of active customer harm (Stripe is still test-mode, no real subscribers exist yet), but it must be resolved before that gate is ever cleared, since flipping to live payments while a sold feature 500s for everyone would be a real problem.
- Files touched: `CLAUDE.md` only (gate updated to 5 conditions, Recent section updated). `docs/ACTIVITY_LOG.md`. No code changes.
- Validation: N/A — read-only investigation (route/component read, `git log --follow` + `git show` on the relevant commits).
- Risks + rollback: None — no mutating changes.
- Blockers/human actions needed: Decide next session which path to take for `AI_GATEWAY_API_KEY`/`AI_GATEWAY_URL` (real Lovable key vs. migrate provider) before the Stripe live-mode gate can be cleared.

## 2026-09-07 (Milestone 3) — RLS & Auth Audit: read-only findings, then fix both confirmed server-side gaps
- Summary: Full read-only RLS/auth audit per 3 explicit asks, followed by 2 approved fixes once findings were reviewed.
  **(1) Self-referencing RLS recursion risk** — pulled all 63 live policies (`pg_policies`) plus all 3 `SECURITY DEFINER` functions (`has_role`, `handle_new_user`, `rls_auto_enable`). Zero policies self-reference their own table via a direct subquery (the exact pattern that caused the 2026-08-30 `user_roles` recursion bug) — every "parent manages X" table subqueries `students` (a different table whose own policy is a plain `parent_id = auth.uid()`, terminating the chain), and every admin check routes through `has_role()` which is itself `SECURITY DEFINER` and bypasses RLS internally. Also confirmed RLS is actually *enabled* on all 37 public tables (`relrowsecurity = true`), not just present-but-inert. Spot-checked the still-unapplied `admin_notifications`/`impersonation_logs`/educators-system migrations too (since they may get applied eventually) — none self-reference either.
  **(2) Cross-family isolation** — tested live via `SET ROLE authenticated` + `request.jwt.claims`, real RLS enforcement, read-only: as the real test-parent account, `students`/`subscriptions` return exactly their own row; as a made-up unrelated uid, `students`/`subscriptions`/`profiles`/`user_roles` all return nothing. Holds structurally (every predicate keys off `auth.uid()`, no OR-escape), and also protects writes since the `FOR ALL` policies use `USING` as `WITH CHECK` too (Postgres default when the latter isn't specified) — a parent cannot insert/update a row tagged with another family's `student_id`. Co-guardian access confirmed non-functional (no policy on any family-data table references `co_guardians` at all — matches an existing code comment in `AuthContext.tsx` that the co-guardian RPC was never built). Direct student-role login confirmed still dead (nothing creates such a profile) and would fail closed if reactivated (no policy grants access by being the student rather than the parent).
  **(3) Free-vs-paid enforcement** — read `ai-tutor`'s full source: auth → role → 30/hr rate limit → ownership, **no subscription check at all**. Read `WeeklyProgressReport.tsx`/`ReportsPanel.tsx`: both query `daily_plan`/`check_ins`/`achievements`/`reward_points` directly via the client SDK with no edge function in between; RLS on those tables is ownership-only. Confirmed `<SubscriptionGate>` (client-side React wrapper around `useSubscription()`, itself just a client-side `subscriptions` query) is the *only* gate for either feature. Conclusion: any authenticated parent — subscribed or not — got full access by calling the REST API or `ai-tutor` directly, bypassing the UI. Also surfaced in passing: both report components still reference the same stale legacy `daily_plan` columns fixed elsewhere this milestone.
  **Fixes approved and shipped**, in order:
  - **`ai-tutor` subscription gate**: added a check mirroring `useSubscription.ts`'s `isActive` exactly (trialing/active, or `past_due` within a 7-day grace period vs `current_period_end`). While wiring this up and live-testing, discovered `ai-tutor` was never actually deployed before (confirmed via `list_edge_functions` — this was its first-ever deploy, version 1, so the paywall gap had no live exposure window) — and its role/ownership checks were themselves broken: `.select("role, display_name, student_id")` against `profiles`, which has never had a `role` or `student_id` column (role lives in `user_roles`; this function predates the schema redesign like everything else this milestone), and the ownership check queried `students` by the legacy text `student_id` column with a uuid value from the real caller (`TutorChat.tsx` always sends `selectedStudentId`, the modern `students.id` uuid) — meaning even a correctly-subscribed real owner would always get rejected. Fixed both (role from `user_roles`, ownership by `students.id`) so the feature is actually usable, not just correctly gated in theory. Live-tested end to end via `curl` with real JWTs: no-subscription account → `402`; subscribed real owner with a foreign/made-up `studentId` → `403`; subscribed real owner with their own `studentId` → passes every gate through to the AI gateway call (fails there only on a separate, out-of-scope `AI_GATEWAY_API_KEY` config question, not touched).
  - **Weekly reports gate**: recommended (and, once approved, built) a new `weekly-report-data` edge function rather than adding a subscription check to `daily_plan`/`check_ins`/`achievements`/`reward_points`'s RLS — those tables also power the always-on daily experience (`TodayBlocks`, `ActivityFeed`, `CheckInForm`, `BadgesPanel`), none of which are subscription-gated anywhere, so an RLS-level gate would have locked non-subscribers out of the entire app, not just reports. The new function mirrors `ai-tutor`'s shape (auth → subscription gate → ownership check via `students.id` → fetch via service role) and accepts `{studentId, startDate?, endDate?, status?}`, returning all 4 resource arrays for whichever window the caller needs. Both `WeeklyProgressReport.tsx` (1 combined call replacing 4 separate direct queries) and `ReportsPanel.tsx` (3 calls replacing 3 direct queries) now call it instead of querying tables directly — their existing chart/stat logic was left untouched. Live-tested the same way: no subscription → `402`, foreign `studentId` → `403`, real owner → `200` with real data.
  - **Stale `daily_plan` columns**: fixed in both report components alongside the gate work (same real schema as `ActivityFeed`/`AdminMessages` earlier this milestone — `planned_date` not `plan_date`, no `self_rating`/`time4learning_score`/`block_order`/`notes` at all). Dropped the avg-score/avg-focus UI in both components since that data was never real, consistent with how the other stale-column fixes this milestone were handled.
- Files touched: `supabase/functions/ai-tutor/index.ts` (subscription gate + role/ownership fix), `supabase/functions/weekly-report-data/index.ts` (new), `src/components/WeeklyProgressReport.tsx`, `src/components/ReportsPanel.tsx`, `CLAUDE.md`, `docs/ACTIVITY_LOG.md`. Remote: both edge functions deployed (`ai-tutor` v1 then v2 same session; `weekly-report-data` v1).
- Validation: `npx eslint`/`npx tsc --noEmit` clean on all 3 touched frontend/shared-type files (one `useMemo` dependency warning I introduced was cleaned up before finishing). Both edge functions live-tested via direct `curl` with real JWTs for the fixture accounts, covering: no subscription (402), foreign/invalid studentId (403), and real subscribed owner (200 / passes through to next stage). Both commits confirmed deployed via Vercel MCP (`state: READY`, `target: production`).
- Risks + rollback: `ai-tutor` and `weekly-report-data` changes are additive/corrective with no destructive paths (no new writes beyond what `ai-tutor` already did for conversation history). Revert via `git checkout` on the touched files, or redeploy the prior function version via Supabase if needed (source is in git history either way). No RLS or migrations changed this session — the weekly-reports gate was deliberately built as a new function specifically to avoid touching shared-table RLS.
- Blockers/human actions needed: none from this pass. Still open, unrelated to this audit: `admin_notifications`/educators system (deliberately held), `RESEND_API_KEY`, leaked-password-protection toggle, and the `AI_GATEWAY_API_KEY`/`AI_GATEWAY_URL` secrets needed for `ai-tutor` to actually reach the LLM (surfaced as a 500 during live-testing, out of scope for this audit).

## 2026-09-07 (4th follow-up) — Fix students.grade_level and messages_log.timestamp/type/recipient, close out Milestone 2
- Summary: The last 2 confirmed active bugs from the prior turn's log cross-check.
  (1) **`students.grade_level`.** Confirmed via the original legacy schema migration (`20260305014211_...sql`) that `grade_level INTEGER NOT NULL DEFAULT 7` was a real column on the single-family prototype's `students` table, and was simply never carried over when the table was redesigned to the current uuid/`parent_id` shape (the same redesign that dropped `daily_plan`'s old columns, fixed last turn). Confirmed it's still genuinely wanted, not abandoned: 6 live components read or write it (`AddStudentFullForm.tsx`, `AddStudentQuickCreate.tsx`, `StudentSwitcherDropdown.tsx`, `StudentProfileCard.tsx`, `AdminStudents.tsx`, `DadPanel.tsx`). Checked whether any of the several *other* migrations mentioning `grade_level` (views/functions like `get_educator_student_summary`) could be reused — no, they're all written against the old legacy schema too (`text` student_id PK, `profile_photo_url`/`monitoring_enabled`/`academic_year` that don't exist on the live table either), so applying them as-is would just create more broken objects. Wrote a fresh, minimal migration instead: `ALTER TABLE public.students ADD COLUMN grade_level integer;` — nullable, no default, so the one existing real student row doesn't get silently assigned a guessed grade. Verified live via `information_schema.columns`. No frontend changes needed — the forms already send the right field name; they just needed the column to exist.
  (2) **`messages_log.timestamp`/`.type`/`.recipient`.** Confirmed live schema (`information_schema.columns`): `id, parent_id, channel, message_type, status, sent_at` — user's guess (`sent_at`/`message_type`) was exactly right. Reading the full `AdminMessages.tsx` file turned up a third stale reference beyond the two named: `recipient` (text) doesn't exist either — the redesigned table replaced it with `parent_id` (uuid), with no `content`/`provider_message_id` columns either (neither referenced in this file, so no further issue). Fixed all 3: the 2 count queries and the main list query's `.eq()`/`.order()` calls, plus the 3 render-time property reads (`m.type`→`m.message_type`, `m.timestamp`→`m.sent_at`, `m.recipient`→`m.parent_id`, relabeling that column header from "Recipient" to "Parent" since it now shows a uuid rather than a contact address). Did not touch the `status`/`"sos"` filter *values* (channel values also changed case — old schema had `WhatsApp`/`Email`/`SMS`, live `messages_log_channel_check` constraint requires lowercase `telegram`/`whatsapp`/`push`/`email` — not asked about this turn, flagging only). Showed both diffs before committing, per instruction.
- Files touched: `src/pages/admin/AdminMessages.tsx`, `CLAUDE.md`, `docs/ACTIVITY_LOG.md`. Remote-only: `add_grade_level_to_students` migration.
- Validation: `npx eslint`/`npx tsc --noEmit` clean on `AdminMessages.tsx`. `grade_level` column existence confirmed via `information_schema.columns` post-migration. Both commits confirmed deployed via Vercel MCP (`state: READY`, `target: production`): `AdminMessages.tsx` fix → `a5ca229` → `dpl_23GCcm2saFEoEm4cGpWRRspu2p29`.
- Risks + rollback: Migration is additive-only (nullable new column), reversible via `ALTER TABLE public.students DROP COLUMN grade_level;`. `AdminMessages.tsx` change is a pure column-name/property-name correction with no behavior change beyond actually working; revert via `git checkout` on the one file.
- Blockers/human actions needed: (1) `messages_log`'s `status`/channel filter *values* may also be stale relative to the redesigned schema (case changed from `WhatsApp`/`Email`/`SMS` to lowercase) — not fixed, not asked about, flagging for a future pass. (2) Milestone 2 is now considered closed from this session's side — the only remaining open items are the deliberately-held `admin_notifications`/educators system (your explicit scope decision) and the pre-existing, tool-inaccessible items (`RESEND_API_KEY`, leaked-password-protection toggle).

## 2026-09-07 (3rd follow-up) — Deploy useRoleSwitcher/ActivityFeed fixes, re-run the full 2026-08-30 diagnostic live-log cross-check
- Summary: Two instructions.
  (1) **Committed, pushed, deployed the `useRoleSwitcher.ts` fix** from the prior turn (`ebd452b` → `dpl_JC63M6dXZLr2ZjEHaHtQJ1xmJvZC`, confirmed `READY`/`target: production`, aliased to `www.independentmindsedu.org`).
  (2) **Fixed `ActivityFeed.tsx`'s `daily_plan` fallback query.** Confirmed the real schema first (`information_schema.columns`): `daily_plan` has `id, student_id (uuid), subject, title, planned_date, status, actual_start, actual_end, created_at` — no `plan_date`, `block_order`, `self_rating`, `time4learning_score`, or `notes` at all (bigger gap than the single renamed-date-column issue reported last session). Corrected the select list to the 5 real columns, `plan_date`→`planned_date`, `block_order`→`created_at` for ordering (no equivalent column exists), and removed the star-rating/score/notes UI since that data was never real. Showed the diff before committing per instruction. Lint + `tsc --noEmit` clean. Committed+pushed+deployed (`00d7ce8` → `dpl_C66cGsK47K42qHp2fKcCHee2qZ7m`, confirmed `READY` on `www.independentmindsedu.org`).
  (3) **Re-ran the log cross-check** (`query_logs` against `postgres_logs` for `does not exist` and `edge_logs` for 4xx `/rest/v1/*` paths, grouped/counted rather than sampled this time to catch low-volume real traffic, not just my own test spam). Found real production traffic (a genuine authenticated admin session, `sb.auth_user ae29fe11-...`, browsing the admin panel 22:05–22:08 today) hit several still-broken pages:
    - **3 previously unclassified missing tables**, all following the exact same pattern as everything else this milestone (migration exists in the repo, never applied): `merge_requests` (`AdminUsers.tsx` — migration `20260321181858_...sql`), `dns_monitor_history` (`DnsHistoryPanel.tsx` — migration `20260405044349_...sql`), `admin_sent_notifications` (`AdminNotificationCenter.tsx` — migration `20260707234059_...sql`). None touched — not asked to fix these, reporting only.
    - **2 new active 42703s**: `students.grade_level` (doesn't exist on `students` at all) from `AddStudentFullForm.tsx:171`'s insert — breaks adding a student via the full form; `messages_log.timestamp`/`messages_log.type` (real columns are presumably `sent_at`/`message_type` per the original diagnostic's own note) from `AdminMessages.tsx` lines 20/33. Neither fixed — not asked to.
    - **`profiles.role` does not exist** (1 occurrence) — traced to `AdminNotificationCenter.tsx:157`'s `.select("id, display_name, role, language_pref, last_active_at, student_id")` against `profiles`, which has none of `role`/`last_active_at`/`student_id` as real columns. This page is part of the `admin_notifications` feature already held per your explicit scope decision, so bundled there rather than treated as a new standalone item.
    - **`profiles.username` (16 occurrences) ran for ~30 hours *after* the `AdminBilling.tsx` fix was already deployed** (fix deployed 2026-09-06T13:16:56Z per `dpl_ATsmV7xkoh1FGRRQ6dKQ4dqJ8jJd`; errors ran 2026-09-06T23:25:32Z–2026-09-07T21:36:23Z, then stopped on their own with nothing since). Confirmed no other code path currently references `profiles.username` (`AdminBilling.tsx` genuinely has it removed; `Index.tsx`'s `profile?.username` is a harmless client-side property read on a type that never had the field, not a network call). Most likely explanation, not fully proven: this app ships as a PWA (`vite-plugin-pwa`) — a service-worker-cached old bundle continuing to run in users' browsers until it naturally refreshed would exactly explain a real fix taking ~30h to actually stop erroring for real traffic. Corrected the stale "(code fix, needs deploy)" note on the 2026-09-06 `CLAUDE.md` Recent line, since it's now confirmed deployed.
    - **Everything previously fixed this milestone is confirmed clear going forward**: `activity_logs` 404 (637 occurrences 2026-09-06T23:25–2026-09-07T22:05, stopped the moment the migration was applied this session), `platform_errors`/`user_feedback` 404s (small bursts right before their migrations landed, zero since), `admin_notifications`/`educators`/`impersonation_logs` 404s (still firing as expected — deliberately held).
- Files touched: `src/hooks/useRoleSwitcher.ts` (committed this turn), `src/components/ActivityFeed.tsx`, `CLAUDE.md`, `docs/ACTIVITY_LOG.md`. No new migrations or function deploys this turn (all DB-side work was from prior turns).
- Validation: `npx eslint`/`npx tsc --noEmit` clean on `ActivityFeed.tsx`. Both deploys confirmed via Vercel MCP (`state: READY`, `target: production`, alias includes `www.independentmindsedu.org`). Log cross-check used grouped counts with `min`/`max` timestamps (not just the most recent N rows) specifically to avoid the mistake of only seeing my own test traffic, as flagged as a risk last session.
- Risks + rollback: Both commits are normal, revertible git commits; Vercel auto-deploys a `git revert` the same way. No DB changes this turn.
- Blockers/human actions needed: (1) Decide whether to fix `students.grade_level` (AddStudentFullForm), `messages_log.timestamp`/`type` (AdminMessages), and/or apply the 3 newly found missing-table migrations (`merge_requests`, `dns_monitor_history`, `admin_sent_notifications`) — none fixed this turn, all follow the identical "migration exists, never applied" pattern as the rest of this milestone. (2) `profiles.role`/`last_active_at`/`student_id` in `AdminNotificationCenter.tsx` will need fixing whenever `admin_notifications` is revisited (it's broken independent of the missing table). (3) The PWA-cache-staleness theory for the delayed `profiles.username` fix rollout isn't fully proven — worth keeping in mind for any future "I already deployed the fix, why are users still hitting it" confusion.

## 2026-09-07 (2nd follow-up) — Commit+deploy the studentId fix, remove the redundant educators-table check causing a 404 on every session
- Summary: Two independent instructions.
  (1) **Committed and pushed the 3 frontend files from the prior follow-up.** Checked actual practice first (not just the written CLAUDE.md rule) — `git log` shows the last 5+ fix commits, including the direct `AdminBilling.tsx` precedent this task was asked to mirror, were all committed straight to `main`, so followed that same pattern rather than opening a branch. Staged only `TodayBlocks.tsx`/`Index.tsx`/`EducatorDashboard.tsx` (left `.env.test`, `CLAUDE.md`, `docs/ACTIVITY_LOG.md`, and the pre-existing untouched `supabase/functions/*` changes out, matching how the `AdminBilling` commit itself only touched its one file). Pushed to `origin/main` (`f289a94`). Confirmed via Vercel MCP (`list_projects` → `lek-l-fasil-companion-18`, linked to `amiralda/independentminds`; `list_deployments`): the newest deployment `dpl_9wbPLgVRMuM4RjA7oTUWb6HmErTm` has `githubCommitSha: f289a94...`, `state: READY`, `target: production` — the auto-deploy fired and completed.
  (2) **Investigated the unconditional `educators` 404** (reported live in the prior session's smoke test). Traced it to `useRoleSwitcher.ts` (used by `Index.tsx` and others on every authenticated page) — its `fetchRoles()` effect runs two separate educator checks: one against `user_roles` (works, table exists) and a second, separate one directly against `public.educators` (the missing table) as a fallback signal. Checked every writer of `educators` project-wide (`grep` across `supabase/functions`) — only `accept-educator-invite` ever inserts a row there, and it always grants the matching `'educator'` row in `user_roles` in the same function call (verified by re-reading that function's code, logged in the 2026-09-06/07 sessions). This makes the separate `educators` check provably redundant in every case the system can currently produce, not just conditionally relevant — so removed it outright (rather than adding a guard that would still fire for the common non-educator case) and left a one-line comment explaining why. This directly answers the user's question: it doesn't need the full educators migration to fix, and it *was* a one-line-scale change, just a removal rather than a conditional guard.
- Files touched: `src/hooks/useRoleSwitcher.ts` (local, uncommitted — not pushed this session, pending user go-ahead), `CLAUDE.md`, `docs/ACTIVITY_LOG.md`. Remote: none this half (the commit/deploy in item 1 covers the prior session's already-applied DB changes; no new migrations or function deploys this time).
- Validation: `npx eslint src/hooks/useRoleSwitcher.ts` — clean. `npx tsc --noEmit` (full project) — clean. Deploy confirmed via Vercel MCP `list_deployments` matching the pushed commit SHA with `state: READY`/`target: production`. The `useRoleSwitcher.ts` fix itself was not exercised live (would need a fresh deploy + smoke test, not done this session) — confidence comes from the exhaustive writer-check (only one function writes `educators`, and it always writes `user_roles` too) rather than a live test.
- Risks + rollback: Commit/push — a normal revertible git commit; Vercel would auto-deploy a `git revert` the same way. `useRoleSwitcher.ts` change — pure removal of a redundant, always-failing query; no behavior change in any case the current (educators-table-less) system can produce, and forward-compatible with the real educators migration since `user_roles` will always carry the same signal once that system exists. Revert via `git checkout` on the one file.
- Blockers/human actions needed: (1) `useRoleSwitcher.ts` fix is local/uncommitted — user did not explicitly ask for this one to be pushed (unlike item 1), so it wasn't; confirm if it should be committed+deployed too. (2) Still open from the prior session: `ActivityFeed.tsx`'s stale `daily_plan` fallback column names, and the 2 fixture test accounts existing permanently until deleted.

## 2026-09-07 (follow-up) — Create test fixture accounts, apply subject_tracks/activity_logs migration, fix profile.studentId bug, re-run smoke test with real screenshots
- Summary: Follow-up to the same-day migration/deploy session, executing the user's 4 remaining instructions.
  (1) **Created both `.env.test` fixture accounts.** Queried `handle_new_user()`'s trigger body first (requires `raw_user_meta_data->>'adult_confirmed' = true` or it raises; auto-inserts a `profiles` row + a `user_roles` row with role `'parent'` for every new user). Inserted both accounts directly into `auth.users` via `execute_sql` (no real service-role key exists locally to use the GoTrue admin API — confirmed `.env.test`'s `SUPABASE_SERVICE_ROLE_KEY` is a placeholder, not a real secret) with `email_confirmed_at = now()` and the required metadata. Granted `test-admin@independentminds.test` the `admin` role via a plain `insert into user_roles` (in addition to the trigger's automatic `parent` row — left as-is, matches how every account works, not a broadening). Attached one `students` row (`student_id` label `TEST-0001`) and one `subscriptions` row (`plan_key: plus`, `status: active`) to the parent account. **Hit a real bug creating these**: first login attempt returned `500 {"code":"unexpected_failure","message":"Database error querying schema"}` — root-caused via `auth_logs` to `error finding user: sql: Scan error on column index 3, name "confirmation_token": converting NULL to string is unsupported` — GoTrue's Go driver scans several `auth.users` token columns (`confirmation_token`, `recovery_token`, `email_change_token_new`, `email_change`) as non-nullable strings, but my direct INSERT left them as real SQL `NULL` (a normal signup/GoTrue-API-created row always sets these to `''`). Fixed via `UPDATE auth.users SET confirmation_token = coalesce(confirmation_token,''), ...` for both rows; verified via direct `curl` against `/auth/v1/token` (200, real JWT returned) before re-running Playwright. Wrote both generated passwords only to `.env.test` (never printed in chat), leaving the placeholder `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`/`PLAYWRIGHT_BASE_URL` values untouched (out of scope, user is fixing `.env` separately).
  (2) **Applied the `subject_tracks`/`activity_logs` migration, corrected.** Before applying, checked what pattern the *other* uuid-converted student-scoped tables (`daily_plan`, `check_ins`, `achievements`, `ai_conversations`) actually use today via `pg_policies` — found they were fully rewritten (not just column-typed) to a single `FOR ALL TO public USING (student_id IN (SELECT students.id FROM students WHERE students.parent_id = auth.uid()))` policy, replacing the old `get_my_student_id()`-based text policies entirely. Applied `subject_tracks`/`activity_logs` with `student_id uuid references students(id)` (matching the fix requested) **and** rewrote their RLS to this same current "parent manages X" pattern instead of keeping the original migration's `get_my_student_id()`/`get_my_role()` policies — the original policies would have been a hard type error (`uuid = text`, no implicit cast) on every query had they been kept as-is with a uuid column, breaking the table for every user including parents, not just returning empty results. Flagged for later (not fixed, out of scope): the still-unapplied `educators` migration's `educator_reads_subject_tracks` policy references `subject_tracks.student_id` as text via `is_my_educator_student()` — will need the same uuid reconciliation whenever that system is revisited.
  (3) **Fixed the `profile?.studentId` bug — bigger than initially scoped, real and currently live (not the dead-branch case reported last session).** Traced deeper this time: `Profile` (the actual TS interface in `AuthContext.tsx`) has no `studentId` *or* `student_id` field at all — only `displayName`/`role`/`languagePref`/`onboardingComplete`. `fetchStudents()` (which populates the real `students`/`selectedStudentId` state) only runs `if (profile.role === "parent")`; nothing in the app ever creates a `profiles.role === 'student'` account (confirmed via grep — no signup/admin flow sets it), so the `actualRole === "student"` direct-login branch in `Index.tsx` is dead code today, exactly as flagged, but **`TodayBlocks.tsx` was a separate, much bigger, live problem**: it derives its own `studentId` internally via `useAuth()` + `profile?.studentId` (always `undefined`) instead of receiving it as a prop like `ActivityFeed` already does. This meant, for every parent, on every block completion: the `activity_logs` insert/upsert, the streak-bonus/category-bonus/challenge-progress calls, and the `StudentRecords` panel were **all silently no-op'd** (gated behind `if (studentId)` which was always false) — separate from and worse than the reported "badge check falls back to hardcoded CHRIS" symptom (line 54 had a `"CHRIS"` fallback; the other 7 usages at lines 116/154/196/383 had none). Fixed by adding a `studentId: string | null` prop to `TodayBlocks`, removing the `useAuth`/`profile` dependency entirely (verified nothing else in the file used `profile`), and updating both callers (`Index.tsx`: `studentId={studentId}`, now itself simplified from a broken ternary to `const studentId = selectedStudentId;` since the removed branch was already dead-equivalent to `selectedStudentId`'s null default; `EducatorDashboard.tsx`: `studentId={selectedStudentId}`) — same category/shape as the `AdminBilling.tsx` fix (remove a reference to something that never worked, verify it's behavior-preserving where reachable, don't build new functionality).
  (4) **Re-ran the full smoke test against production with real credentials**, via a local ad-hoc Playwright script (not committed) using `chromium` installed this session, capturing console/network + screenshots for all 3 target surfaces. Confirmed via screenshots + logs: student "today"/activity-feed view no longer 404s on `subject_tracks`/`activity_logs` (renders "No activities logged today" cleanly instead of erroring — the `daily_plan` fallback query inside `ActivityFeed.tsx` still 42703s on stale column names `plan_date`/`self_rating`, hint says `planned_date`, but the component's own guard prevents this from surfacing as a crash — **not fixed, flagged only, out of today's scope**); admin panel loads real data (`Total Students: 1`, matching the fixture) — confirming the admin role grant works end-to-end; feedback widget now succeeds end-to-end (`"Thank you for your feedback!"` toast, row verified in `user_feedback` via `execute_sql`, then deleted). New finding, not previously known: the `educators` table 404 (`PGRST205`) fires on **every** authenticated page load across all 3 surfaces tested (parent, admin, feedback) — broader than "gated behind actively using educator features" as previously assessed; some top-level hook (likely `useRoleSwitcher.ts`) queries it unconditionally on every session.
  Note: the frontend code fix (item 3) is **local/uncommitted** — same caveat as the `AdminBilling.tsx` precedent — the smoke test's "no more crash" result reflects the DB-side migration (item 2) working; the `TodayBlocks`/`Index.tsx`/`EducatorDashboard.tsx` fix itself needs a commit + Vercel deploy before it's live, and wasn't (and couldn't be) exercised by this test since no `daily_plan` blocks existed for the fixture student to mark complete.
- Files touched: `src/components/TodayBlocks.tsx`, `src/pages/Index.tsx`, `src/components/EducatorDashboard.tsx` (all local, uncommitted — needs deploy), `.env.test` (2 password lines only), `CLAUDE.md`, `docs/ACTIVITY_LOG.md`. Remote-only: `create_subject_tracks_activity_logs_uuid` migration; `auth.users`/`user_roles`/`students`/`subscriptions`/`profiles` rows for the 2 fixture accounts (via `execute_sql`, not a tracked migration — intentionally ad-hoc test data, not schema).
- Validation: `npx eslint` (targeted: the 3 edited files) — clean. `npx tsc --noEmit` (full project) — clean. DB: `information_schema.columns` re-checked (`subject_tracks`/`activity_logs.student_id` now `uuid`); direct `curl` login test for both fixture accounts (200, real JWT) before and after the token-column fix; full Playwright run against production with screenshots (`01-parent-today.png` through `05-feedback-after-submit.png`, saved to session scratchpad) and console/network capture; `user_feedback` test row confirmed via `execute_sql` then deleted; test `auth_failures` row from the prior session's function test was already cleaned up.
- Risks + rollback: New migration is additive-only (two new tables), reversible via `DROP TABLE`. Fixture accounts/data are isolated (`.test` email domain, obviously-fake Stripe IDs) and easy to remove (`DELETE FROM auth.users WHERE email IN (...)` cascades via FKs to `profiles`/`user_roles`/`students`/`subscriptions`). Frontend fix is a pure prop-threading change with no behavior change on any currently-reachable path (verified: the removed `profile?.studentId` branches always evaluated to the same value the new code produces); revert via `git checkout` on the 3 files if needed.
- Blockers/human actions needed: (1) Commit + deploy (Vercel) the 3 frontend files before the `TodayBlocks` activity-logging/streak/bonus fix is actually live — not done this session (only commit when explicitly asked). (2) `daily_plan` fallback query in `ActivityFeed.tsx` uses stale column names (`plan_date`→`planned_date`, `self_rating` doesn't exist) — flagged, not fixed. (3) `educators` table 404 fires on every authenticated page load, not just educator-feature usage — worth knowing before deciding priority when the educators system migration is eventually applied. (4) The two fixture accounts + their student/subscription data now exist permanently on the live project until someone deletes them — fine for ongoing e2e use, but worth knowing they're not auto-expiring.

## 2026-09-07 — Classify the 9 missing tables/5 undeployed functions from the 2026-08-30 diagnostic; apply 2 of 9 migrations + deploy 2 of 5 functions
- Summary: Follow-up to the 2026-08-30 diagnostic's PGRST205/missing-table findings, in priority order set by the user (subject_tracks+activity_logs, admin_notifications, user_feedback, educators, platform_errors) plus the 5 undeployed edge functions. Classified all 9 tables: every one has an existing, unapplied migration backing a fully-built feature (frontend UI + RLS + in some cases edge functions) — none are dead/abandoned code. Confirmed via `list_edge_functions` that only 8 functions are deployed project-wide (of ~37 in the repo) and via `cron.job` that only the 4 Aug-30-rewritten jobs are scheduled — `hourly-monitor` was never scheduled even before being undeployed.
  Per the user's follow-up instructions this session:
  (1) **Applied `user_feedback` migration (self-contained)** — split out of the combined `20260405052942_...sql` file (which also creates `platform_errors`) into its own `apply_migration` call (`create_user_feedback`), verbatim CREATE TABLE + RLS + 2 indexes. Verified live via `pg_class.relrowsecurity`.
  (2) **Resolved the subject_tracks/activity_logs join-key question, did NOT apply that migration.** Traced the actual call chain: `Index.tsx:58` sets `studentId` from `selectedStudentId` (parent/admin path) or `profile?.studentId` (student path); `AuthContext.tsx:198` sets `selectedStudentId = found.id` — the **uuid** `students.id`, matching the Aug-30 fix's convention for `daily_plan`/`check_ins`/etc. But the `subject_tracks`/`activity_logs` migration (`20260308193529_...sql`) FKs `student_id` as **text** to `students.student_id` (the legacy label), predating and un-touched by the Aug-30 fix. Applying it unmodified wouldn't error — it would silently return zero rows forever, since the query always compares the text column against a uuid-shaped string. Needs `student_id` changed to `uuid references students(id)` before applying. Also found, as a side discovery not in scope to fix: `profile?.studentId` (camelCase) referenced in `Index.tsx:58` and `TodayBlocks.tsx` doesn't exist on the `Profile` type (the real field is `profile.student_id`, snake_case, populated from `students.student_id` per `AuthContext.tsx:186`) — meaning a student who logs in directly (not via parent "view as student") gets `studentId = undefined` today, separate from the migration question.
  (3) **Held `admin_notifications` and the 7-table educators system per explicit instruction** — no action taken, still missing live. Confirmed `admin_notifications`'s own trigger depends on `impersonation_logs` (also missing live) — applying `admin_notifications` alone won't be sufficient when the user does revisit it.
  (4) **Deployed the monitoring bundle**: applied `platform_errors` migration standalone (split from the same combined file as `user_feedback`), then deployed `track-error` (`verify_jwt: true` — matches its own internal Bearer-token check; live-tested with no auth header → correctly `401 UNAUTHORIZED_NO_AUTH_HEADER`, confirming the gateway-level JWT check works) and `track-auth-failure` (`verify_jwt: false` — intentionally public, fires on failed login before any session exists; live-tested via direct `curl` with a throwaway test email → `202 {"ok":true}`, confirmed the row landed in `auth_failures` with masked IP, then deleted the test row). Did NOT deploy `hourly-monitor` or `beta-track` per instruction (both write to the still-missing `admin_notifications`).
  (5) **Password reset for `.env.test`'s parent/admin test accounts — blocked, not done.** Queried `auth.users` on the live project (`gyvjcwuwfwrwwwnuwlex`) for `test-parent@independentminds.test` / `test-admin@independentminds.test` — **zero rows**. These aren't stale passwords, the accounts don't exist at all. Did not unilaterally create them (especially the admin one — granting an admin role to a freshly-created account isn't a decision to make silently). Also discovered, incidentally: `.env.test` and `.env.example` are both tracked in git (not gitignored), but `.env.test`'s `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` are placeholder values (`your-project.supabase.co`), not real secrets — so no real service-role key is actually committed; real test-run secrets must be injected via CI env vars. Separately, the local (gitignored) `.env` file's `VITE_SUPABASE_URL` points to project ref `wkvattbvybvgaeobtidl`, which doesn't match the live project (`gyvjcwuwfwrwwwnuwlex`, confirmed as the one the production frontend actually calls, via live network capture during the smoke-test attempt) and doesn't match any of the 4 projects on this account — likely stale from before a project delete+recreate; local `npm run dev` would currently point at the wrong/inaccessible project.
  Smoke test itself (student "today" tab, admin page, feedback widget) could not proceed past login given the above — see blockers.
- Files touched: `CLAUDE.md`, `docs/ACTIVITY_LOG.md` (docs only). Remote-only (via `apply_migration`): `create_user_feedback`, `create_platform_errors`. Remote-only (via `deploy_edge_function`): `track-error` (v1), `track-auth-failure` (v1). No local code files changed; no commits made.
- Validation: `user_feedback`/`platform_errors` existence + RLS confirmed via `pg_class.relrowsecurity` and `list_tables`. Both new edge functions live-tested directly against production via `curl` with the project's real anon key (`track-auth-failure`: 202 + row confirmed in `auth_failures` via `execute_sql`, then cleaned up; `track-error`: 401 unauthorized-rejection confirmed, full authenticated path not exercised since no valid test JWT was available this session).
- Risks + rollback: Both migrations are additive-only new tables (reversible via `DROP TABLE public.user_feedback` / `DROP TABLE public.platform_errors` — data loss limited to whatever's been inserted since). Both edge functions are new deploys (no prior version existed to regress); each fails closed (401) for `track-error` without a valid JWT, and `track-auth-failure` only ever inserts to `auth_failures`, no destructive paths in either. No auth accounts were created or altered.
- Blockers/human actions needed: (1) Decide whether to create the missing `.env.test` parent/admin test fixture accounts (and if so, what role/profile/student data they need — the admin one in particular needs an explicit `user_roles` grant, which shouldn't happen silently); until then the Playwright smoke test against production can't get past login. (2) Confirm/adjust the `subject_tracks`/`activity_logs` migration's join-key type (text→uuid) before it's applied. (3) Check whether the local `.env`'s `VITE_SUPABASE_URL` (`wkvattbvybvgaeobtidl`) is genuinely stale — if so, local dev is currently pointed at an inaccessible/wrong project. (4) `profile?.studentId` vs `profile.student_id` naming mismatch in `Index.tsx`/`TodayBlocks.tsx` — separate bug affecting students who log in directly, not fixed this session (out of scope, flagged only).

## 2026-09-06 — Milestone 2: security fixes (get_advisors) + 42703 bug fixes (from prior diagnostic)
- Summary: Follow-up to the 2026-08-30 diagnostic and security audit. Three approved fixes, applied in order with verification after each.
  (1) **RPC exposure on 3 SECURITY DEFINER functions (applied)**: `get_advisors` still flagged `has_role`, `handle_new_user`, `rls_auto_enable` as callable via `/rest/v1/rpc/...` by `anon`/`authenticated`. Confirmed `handle_new_user` (trigger-only, fires on `auth.users` insert) and `rls_auto_enable` (event-trigger-only, fires on `CREATE TABLE`) are never called from app code; `has_role` is called from `useAdminAuth.ts` but only for the caller's own session (never anonymously). First pass (`revoke execute ... from anon, authenticated`) looked applied but `has_function_privilege('anon', ...)` still returned `true` — root cause: all 3 functions had the default Postgres `GRANT EXECUTE TO PUBLIC` from creation time, which `anon`/`authenticated` inherit regardless of per-role revokes. Second migration explicitly revoked from `PUBLIC` and re-granted `has_role` to `authenticated` only. Verified via `pg_proc.proacl` (anon/authenticated grants gone from `handle_new_user`/`rls_auto_enable`; only `authenticated`+`service_role`+`postgres` remain on `has_role`) and by executing `has_role(<real admin user_id>, 'admin')` under `SET ROLE authenticated`, which returned `true` — confirmed the admin-auth path still works. `get_advisors` re-run confirms the anon/authenticated warnings are gone for `handle_new_user`/`rls_auto_enable`; the remaining `has_role`/`authenticated` warning is the intentional, kept-open path.
  Also confirmed (read-only, no action needed): `search_path` on all 3 functions was already locked down (`has_role`/`handle_new_user` → `SET search_path TO ''`, `rls_auto_enable` → `SET search_path TO 'pg_catalog'`) from an earlier, undocumented pass — CLAUDE.md checkbox was stale and has been corrected.
  Leaked password protection (`auth_leaked_password_protection` WARN) is an Auth-service setting, not reachable via any available SQL/Management-API tool — left for the user to toggle in the Dashboard (Authentication → Sign In / Providers → Password).
  (2) **AdminBilling.tsx querying nonexistent `profiles.username` (code fix, applied locally, NOT yet deployed)**: Confirmed via `information_schema.columns` that `public.profiles` has never had a `username` column (only `display_name` for display purposes). This was actively firing `42703 column profiles.username does not exist` in production (20 occurrences in the prior 24h window, most recent 12:37 today) via the embedded-relation select `profiles:user_id(display_name, username)` at `AdminBilling.tsx:31`, and the query `throw`s on error — breaking the entire Admin Billing page (subscriptions list + status counts) on every load. Removed `username` from the select, the `SubscriptionRow.profiles` type, and the render fallback (`row.profiles?.display_name || row.profiles?.username || row.user_id` → `row.profiles?.display_name || row.user_id`; behavior is unchanged since the `username` fallback branch could never have fired). **This is a frontend code change and requires a deploy (Vercel) before the live 42703 errors for `profiles.username` stop** — the DB-side fixes in this task don't affect this one.
  (3) **`beta_testers` missing `points_earned`/`current_level`/`first_login_shown` columns (schema fix, applied to production, no code deploy needed)**: Investigated whether this was an abandoned feature or a planned one with a missed migration, per user's explicit request before deciding the fix approach. Found `supabase/migrations/20260405040949_84c3f775-7145-451c-bf5e-5410a514a2b2.sql` already existed in the repo with the exact 3 `ADD COLUMN IF NOT EXISTS` statements needed, but `list_migrations` confirmed it was never applied to `gyvjcwuwfwrwwwnuwlex`. Further confirmed the feature is fully built end-to-end and not abandoned: `BetaWelcomeModal.tsx` (onboarding modal, reads/writes `first_login_shown`), `approve-beta-request`/`accept-beta-invite` edge functions (insert new testers with all 3 columns), `beta-progress-email` (computes and writes `points_earned`/`current_level` on schedule), `beta-notify`/`admin-notify` (read `points_earned`/`current_level` for filtering/dashboards) — meaning all 5 edge functions were also silently hitting the same missing-column error, not just `useBetaTester.ts`. Applied the existing migration as-is (additive only, `IF NOT EXISTS` guards, safe defaults: `points_earned integer NOT NULL DEFAULT 0`, `current_level text NOT NULL DEFAULT 'Explorer'`, `first_login_shown boolean NOT NULL DEFAULT false`) rather than simplifying the frontend query, since simplifying would have left the 5 edge functions broken. Verified via `information_schema.columns` (all 3 columns present with correct types/defaults) and by running the exact select shapes used by `useBetaTester.ts`, `beta-notify`, and `admin-notify` directly against `beta_testers` (all succeeded); a transactional insert/update dry-run using the `approve-beta-request`/`beta-progress-email` column shapes got past column resolution and only hit unrelated, expected business constraints (`tester_type` CHECK, `user_id` FK to a real auth user) before rolling back. Re-queried `postgres_logs` for `42703` after the migration timestamp (12:50:41 UTC) — zero occurrences. No code changes needed for this fix; `useBetaTester.ts` and the 5 edge functions already query these exact column names.
- Files touched: `src/pages/admin/AdminBilling.tsx` (code, uncommitted — needs deploy), `CLAUDE.md`, `docs/ACTIVITY_LOG.md`. Remote-only (via `apply_migration`, tracked in Supabase migration history): `revoke_public_rpc_execute_on_internal_functions`, `revoke_public_execute_grant_on_internal_functions`, `beta_testers_add_points_level_first_login`.
- Validation: `npx eslint` (targeted: `AdminBilling.tsx`, `useBetaTester.ts`, `BetaWelcomeModal.tsx`) — clean, no errors. `npx tsc --noEmit` (full project) — clean, no errors. DB-side: `pg_proc.proacl` + `has_function_privilege` re-checked post-fix for the RPC grants; `has_role()` executed under `SET ROLE authenticated` against a real admin user id, returned `true`; `information_schema.columns` re-checked for the 3 new `beta_testers` columns; `query_logs` re-checked for `42703` post-migration (zero new occurrences).
- Risks + rollback: RPC grants — reversible via `grant execute on function ... to anon, authenticated;` (or `to public`) to restore exactly the prior state. `beta_testers` migration — additive only, reversible via `ALTER TABLE public.beta_testers DROP COLUMN points_earned, DROP COLUMN current_level, DROP COLUMN first_login_shown;` (no data loss beyond the new columns themselves, since they didn't exist before). `AdminBilling.tsx` — pure removal of a nonexistent-column reference and its unreachable fallback branch; revert via `git checkout` on that file.
- Blockers/human actions needed: (1) `AdminBilling.tsx` fix is local/uncommitted — needs to be committed, pushed, and deployed before the live `profiles.username` 42703 errors actually stop (DB migration alone doesn't fix a frontend code bug). (2) Leaked password protection still disabled — user is handling this directly in the Supabase Dashboard. (3) The 223 static `PGRST205` (missing table) findings from the 2026-08-30 diagnostic were checked against live `postgrest_logs` for the last 24h and showed zero occurrences — not confirmed active in the observable window, but the log tool caps at 24h so a rarer/already-avoided path wouldn't show even if real; recommend the same live-vs-static cross-check done here (for 42703) be extended to that list before spending further fix effort.

## 2026-08-30 — Fix user_roles RLS recursion + reactivate 4 notification cron jobs
- Summary: Two fixes from the 2026-08-30 diagnostic, done in the order the user requested.
  (1) **RLS recursion (applied)**: Replaced the self-referencing `"admin manages roles"` policy on `public.user_roles` (`EXISTS (SELECT 1 FROM user_roles ...)`) with `USING/WITH CHECK (public.has_role(auth.uid(), 'admin'))`, using the existing `SECURITY DEFINER` `has_role()` function to break the recursion. Verified via `pg_policies` — no behavior change, same authorization logic, just routed through a function that bypasses RLS internally. Applied directly via `apply_migration` (name `fix_user_roles_rls_recursion`), not saved as a local migration file since it was a single ad-hoc DDL statement per the user's "explain then apply" instruction.
  (2) **4 broken cron jobs (applied, pending 2 secrets)**: Discovered mid-task that `morning-reminder`, `checkin-reminder`, `daily-report`, `weekly-badge` edge functions existed locally but were never deployed to `gyvjcwuwfwrwwwnuwlex`, AND the local source was legacy single-family (`student_id = "CHRIS"`) Telegram-only prototype code incompatible with the current multi-tenant/Stripe-billed schema (wrong column names throughout — `daily_plan.plan_date`/`block_order` vs actual `planned_date`, `messages_log.recipient`/`type`/`content` vs actual `parent_id`/`message_type`/no content column, etc.). Rewrote all 4 from scratch against the real schema, per user's spec: Resend email (direct API call, not the undeployed `send-transactional-email`/`process-email-queue` pipeline — confirmed via `execute_sql` that `suppressed_emails`, `email_unsubscribe_tokens`, `email_send_log`, and RPC `enqueue_email` don't exist on this project), "active student" = student whose parent has `subscriptions.status` in `('trialing','active')`, recipient = parent's auth email (students have no login/email of their own), bilingual EN/HT HTML body, WhatsApp/SMS gated behind `plan_key in ('plus','pro')` + `parent_settings.notification_channel` left as an explicit `TODO(whatsapp-sms)` (not implemented, per instructions). `weekly-badge` only sends when there's actual progress (done tasks, points, or achievements) that week. Added migration extending `messages_log_channel_check` to allow `'email'` (was `telegram`/`whatsapp`/`push` only). Installed `pg_net` extension (was missing — root cause of "schema net does not exist" errors) and rescheduled all 4 `cron.job` rows with corrected URLs (`https://gyvjcwuwfwrwwwnuwlex.supabase.co/functions/v1/<slug>`, previously 2 had an unfilled placeholder hostname and 1 was missing `.supabase.co`) and a freshly generated `x-cron-secret` header value. Deployed all 4 functions (`deploy_edge_function`, `verify_jwt: false` — auth is via the custom cron-secret header, matching the `stripe-webhook` pattern). Each function ships as a single self-contained `index.ts` (no `_shared/` cross-file import — the deploy bundler could not resolve a shared `_shared/cron-notify.ts` file across functions, so the ~140 lines of shared logic are duplicated in each of the 4 files instead).
- Files touched: `supabase/functions/morning-reminder/index.ts`, `supabase/functions/checkin-reminder/index.ts`, `supabase/functions/daily-report/index.ts`, `supabase/functions/weekly-badge/index.ts` (all rewritten), `supabase/migrations/20260830190000_messages_log_add_email_channel.sql` (new), `supabase/migrations/20260830190500_fix_notification_cron_jobs.sql` (new), `CLAUDE.md`, `docs/ACTIVITY_LOG.md`. Remote-only: the `has_role()` RLS policy fix (ad-hoc, not saved as a migration file).
- Validation: No `deno` CLI available locally to run `deno check`/`deno lint` on the edge functions (Deno-style `https://esm.sh/...` imports aren't compatible with the project's Node/tsc setup, and `tsconfig.app.json` doesn't include `supabase/functions/`) — validated instead by (a) successful `deploy_edge_function` calls, which bundle/type-check server-side and would reject invalid syntax, and (b) a live smoke test: POSTed to all 4 deployed function URLs with the new `x-cron-secret` header — all 4 correctly returned `401 {"error":"Unauthorized"}`, proving the auth path works (rejects because `CRON_SECRET` is not yet set as a Supabase Edge Function secret — expected, see blockers). RLS fix verified via `pg_policies` re-query showing the new `has_role(...)` expression in both `USING` and `WITH CHECK`. `pg_net` extension and corrected `cron.job` rows verified via `execute_sql`. Frontend `npm run lint`/`tsc --noEmit`/`build` not run — no frontend files were touched this task.
- Risks + rollback: RLS — reversible via `DROP POLICY` + recreate with the original `EXISTS (SELECT 1 FROM user_roles ur WHERE ur.user_id = auth.uid() AND ur.role = 'admin')` USING clause (documented in the 2026-08-30 diagnostic chat). Cron/pg_net — additive only; `cron.unschedule('<job-name>')` disables a job, `drop extension pg_net` would break the jobs again (do not drop). `messages_log` constraint — additive (widens allowed values), rollback = re-run the old 3-value CHECK. Edge functions — each is version 1; previously they didn't exist/weren't deployed, so "rollback" = delete the function via dashboard/CLI if needed. No data was sent (all live invocations returned 401 pre-secret-config), so no parent/student received a real notification during this task.
- Blockers/human actions needed: **Two secrets must be set on the Supabase project (dashboard or `supabase secrets set` — no MCP tool exposes secret-setting) before these jobs will actually run**: (1) `CRON_SECRET` = `<REDACTED: rotated 2026-09-25, value lives in Vault 'cron_secret'>` (must match exactly what's baked into the 4 `cron.job` rows), (2) `RESEND_API_KEY` (a valid Resend API key — unknown whether one already exists for this project; `process-email-queue`/`send-transactional-email` expect the same variable name but those functions aren't deployed either, so this may not be configured yet). Until both are set, the cron jobs will fire on schedule but every call will fail (401 without `CRON_SECRET`, or a logged `"failed"` `messages_log` row without `RESEND_API_KEY`). Also note: there are currently 0 rows in `students` and only 1 row in `subscriptions` on this project, so even once secrets are set, these jobs will initially send 0 emails until real student/subscription data exists — recommend a manual test invocation once secrets are set and at least one active student exists. Diagnostic items #3 (column mismatches) and #4 (missing tables) from the 2026-08-30 diagnostic remain open, explicitly deferred to a future session per the user's request.

### 2026-08-30 (same day, follow-up) — End-to-end smoke test of the notification path
- Summary: User confirmed the Resend domain is verified and DNS (SPF/DKIM/DMARC) is clean, and asked for an end-to-end curl test with an isolated test identity (not their real account). Deployed a temporary diagnostic-only edge function `test-notify-fixture` (setup/cleanup/check/resend-check actions, gated by the same `x-cron-secret`/`CRON_SECRET`) that creates an isolated auth user (email `delivered@resend.dev` — Resend's official sandbox address that never delivers anywhere) plus a `subscriptions` row (`status: active`) and a `students` row, so the real `morning-reminder` function has exactly one active student to notify without touching real user data. First `setup` attempt failed with "Database error creating new user" — root-caused to the `on_auth_user_created` → `handle_new_user()` trigger, which raises `'Adult confirmation required'` and aborts the whole transaction unless `raw_user_meta_data->>'adult_confirmed'` is `true` (by design, not a bug — just missing from the fixture's `createUser` call). Fixed the fixture, retried: setup succeeded, then invoked the real `morning-reminder` (confirmed `CRON_SECRET` works: 401→200), which found the 1 test student but returned `failed:1` (not `sent:1`). Called the fixture's `resend-check` action (calls Resend directly) — returned `{"hasKey": false}`, confirming `RESEND_API_KEY` is **not actually set** on the project despite the user's message. Ran `cleanup`, then independently re-verified via `execute_sql` (not just trusting the fixture's own report) that all 5 rows (`students`, `subscriptions`, `messages_log`, `profiles`, `auth.users`) for the test identity are gone (all counts 0).
- Files touched: none in the repo (the fixture was deployed ad-hoc via `deploy_edge_function`, not written to a local file, since it's throwaway diagnostic tooling — not committed). `docs/ACTIVITY_LOG.md` only.
- Validation: `CRON_SECRET` confirmed working end-to-end (real HTTP calls, 401→200). `RESEND_API_KEY` confirmed NOT working — direct Resend API call from within the edge function runtime returned `hasKey: false`. Cleanup verified independently via `execute_sql` COUNT queries against all 5 affected tables/schemas, not just the fixture's self-reported "deleted" status.
- Risks + rollback: One isolated test auth user + student + subscription were created and fully deleted within this task; independently confirmed empty. No real email was sent (Resend was never actually reached with a working key) and no real user data was touched.
- Blockers/human actions needed: **`RESEND_API_KEY` still needs to be set** on the Supabase project — despite being asked to confirm it, testing shows it is not present (check exact name, no typos/whitespace, value should start with `re_`). The temporary `test-notify-fixture` edge function is still deployed (no MCP tool exists to delete an edge function) — user needs to delete it manually via dashboard or `supabase functions delete test-notify-fixture`; it's low-risk to leave (gated by the same cron secret) but serves no purpose once secrets are confirmed. Once `RESEND_API_KEY` is actually set, re-run the same fixture-based smoke test to confirm `sent:1`.

## 2026-08-30 — Diagnose ~27% Supabase failure rate (read-only)
- Summary: Queried `gyvjcwuwfwrwwwnuwlex` unified logs (`edge_logs`, `postgres_logs`, `storage_logs`) via `query_logs`, plus `pg_policies`, `pg_proc`, `cron.job`, `pg_extension`, and `get_advisors` (all read-only), to diagnose an observed error spike (12 API-gateway errors, 6 storage warnings + 2 errors, 7 Postgres errors in a reported 60-min window). Found the real error window spans back to 2026-08-29 20:43, not just 60 minutes, and identified 4 distinct root causes, none caused by the Stripe billing work: (1) **CRITICAL, live now** — RLS policy `"admin manages roles"` on `public.user_roles` self-references `user_roles` in its own `USING` clause, causing Postgres `42P17 infinite recursion detected in policy for relation "user_roles"` (32 occurrences through 18:30 today) → 500s on every role check (`has_role` RPC / admin auth), directly related to the `useAdminAuth`/`has_role` work in the latest commit. (2) **CRITICAL, silent** — `pg_cron` jobs `morning-reminder-job`, `checkin-reminder-job`, `daily-report-job` call `net.http_post`, but the `pg_net` extension is not installed (`schema "net" does not exist`, 3/day); additionally 2 of the 3 job URLs contain a literal unfilled placeholder hostname and `weekly-badge-job`'s URL is malformed (missing `.supabase.co`) — all parent notification cron jobs have been silently failing every day regardless of the extension issue. (3) **MODERATE** — ~470 `42703 undefined_column` PostgREST errors from frontend queries against stale/mismatched column names on `profiles`, `beta_testers`, `messages_log`, `students`, `check_ins`, `reward_redemptions` (e.g. `profiles.username`/`role`/`onboarding_complete` don't exist; `messages_log.timestamp`/`type` should be `sent_at`/`message_type`). (4) **MODERATE** — ~320 `PGRST205` errors: `platform_errors`, `educators`, `admin_notifications`, `activity_logs`, `user_feedback` tables are referenced by frontend code but do not exist in the schema at all (confirmed via `list_tables`, not a cache issue). Confirmed via `get_advisors`/`pg_proc` that the CLAUDE.md-tracked `search_path` fix is still 2/3 done (`rls_auto_enable` has `search_path=pg_catalog` set; `has_role` and `handle_new_user` still have `proconfig: null`). `storage_logs` showed no real errors in the queried 24h window (only benign mgmt-api health checks) — could not reproduce the "6 warnings + 2 errors" the user saw, possibly a narrower/different window than the 24h cap allowed. No fixes applied — diagnostic only, per user request; recommendations given, awaiting user decision on what to fix and in what order.
- Files touched: `CLAUDE.md`, `docs/ACTIVITY_LOG.md` (docs only — no code/schema changes).
- Validation: N/A (read-only diagnostic; no code changed).
- Risks + rollback: None — no mutating queries run.
- Blockers/human actions needed: User to decide fix order for: (a) `user_roles` RLS recursion — rewrite `"admin manages roles"` policy to use `has_role()` instead of self-referencing `user_roles`; (b) enable `pg_net` extension + fix the 4 broken cron job URLs (2 unfilled placeholders, 1 malformed hostname); (c) reconcile frontend queries with actual column/table names (or add the missing columns/tables if the frontend is the source of truth); (d) finish the still-outstanding `search_path` fix on `has_role`/`handle_new_user` (already tracked in the Stripe live-mode gate checklist).

## 2026-08-30 — Record Stripe live-mode gate decision
- Summary: User explicitly decided to remain on Stripe test mode (`pk_test_.../sk_test_...`) rather than switching to live keys now, after being shown that the current production deploy already has a working test-mode Stripe integration. Set 4 explicit go-live conditions, all of which must be done before live keys are considered: (1) email DNS corrected, (2) Supabase failure rate investigated, (3) `search_path` fixed on the 3 SECURITY DEFINER functions flagged by `get_advisors` (`has_role`, `handle_new_user`, `rls_auto_enable`), (4) final pricing/terms review completed. Added a standing "Stripe live-mode gate" checklist section to `CLAUDE.md` (placed before "Audit findings") so future sessions see it and don't flip to live keys without explicit user sign-off once the checklist is complete.
- Files touched: `CLAUDE.md`, `docs/ACTIVITY_LOG.md` (docs only — no code/schema/env changes).
- Validation: N/A (decision-recording task, no code changed).
- Risks + rollback: None — no mutating changes made; this only documents a decision. To revert, remove the "Stripe live-mode gate" section from `CLAUDE.md`.
- Blockers/human actions needed: Track the 4 gate conditions as separate future tasks; do not touch `pk_live_`/`sk_live_` keys in Vercel/Supabase until all 4 are checked off and the user confirms.

## 2026-08-30 — Verify Stripe backend live state
- Summary: User reported Stripe secrets were entered into Supabase Secrets earlier and asked to confirm. No MCP tool lists secret values/names, and local `supabase` CLI (v2.115.0) is not authenticated (`supabase login` not run), so verified indirectly: (1) confirmed `update_updated_at_column()` exists in both `public` and `storage` schemas via `execute_sql`; (2) confirmed all 5 expected migrations show as applied via `list_migrations`; (3) confirmed all 3 Stripe edge functions (`create-checkout-session`, `create-portal-session`, `stripe-webhook`) are `ACTIVE` via `list_edge_functions`; (4) read `stripe-webhook` source, which returns `500 {"error":"Stripe env not configured"}` before signature verification if `STRIPE_SECRET_KEY` or `STRIPE_WEBHOOK_SECRET` is unset — then sent a live POST with a bogus `stripe-signature` header and got `400 {"error":"Invalid webhook signature"}`, proving both secrets are set and non-empty (the request reached and failed at the signature-check step, not the earlier env-check step). Also ran `get_advisors` (security) as a routine check: same 8 pre-existing warnings as the 2026-08-23 entry (SECURITY DEFINER functions without fixed `search_path`, leaked-password-protection disabled), none new.
- Files touched: `CLAUDE.md`, `docs/ACTIVITY_LOG.md` (docs only — no code/schema changes this task).
- Validation: N/A (verification/read-only task, no code changed). Live probe of `stripe-webhook` returned expected 400, not 500.
- Risks + rollback: None — no mutating changes made.
- Blockers/human actions needed: User confirmed (verified earlier in this same session, outside tool-observable state) that the Stripe Dashboard webhook endpoint already points at `gyvjcwuwfwrwwwnuwlex`'s `stripe-webhook` function with the correct 6 events subscribed (`checkout.session.completed`, `customer.subscription.created/updated/deleted`, `invoice.paid`, `invoice.payment_failed`) — matches what `stripe-webhook/index.ts` handles. `search_path`-mutable SECURITY DEFINER functions (`has_role`, `handle_new_user`, `rls_auto_enable`) and disabled leaked-password-protection remain open security advisories, unaddressed by design (out of scope for this task).

## 2026-08-30 — Verify VITE_STRIPE_PUBLISHABLE_KEY in latest Vercel deploy
- Summary: No MCP tool exposes Vercel env var values, so verified by proof instead of trust: fetched the live production `index.html` from `www.independentmindsedu.org`, resolved the lazy-loaded `Pricing-BOMQb0X-.js` chunk (found via a chunk-name grep in the main bundle, since `src/pages/Pricing.tsx` reads `import.meta.env.VITE_STRIPE_PUBLISHABLE_KEY` at module scope and Vite inlines it at build time), and confirmed a literal `pk_test_51TsSQNGR...` string is baked into that chunk. Cross-checked via `get_project`/`get_deployment` (Vercel MCP) that this chunk belongs to the current production deployment `dpl_3jjYz7cVEpsecHxXSuTZgnfANcou` (commit `64a9cc5`, redeployed 2026-08-30 14:09 UTC, `readyState: READY`, aliased to `independentmindsedu.org`/`www.independentmindsedu.org`). Flagged to user: the key is `pk_test_...` (Stripe test mode), not `pk_live_...` — checkout will work but no real charges will process until swapped for a live key (and `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET` on Supabase are confirmed live too); awaiting user confirmation on whether test mode is intentional for now.
- Files touched: `docs/ACTIVITY_LOG.md`, `CLAUDE.md` (docs only).
- Validation: N/A (verification task, no code changed).
- Risks + rollback: None — no mutating changes made.
- Blockers/human actions needed: User to confirm whether `pk_test_...` is intentional (pre-launch QA) or needs swapping to `pk_live_...` + live Supabase secrets before real payments can process.

## 2026-08-23 — Merge T1-T5 to main + Supabase backend deploy
- Summary: Installed the Supabase CLI (v2.115.0, not authenticated — MCP used instead for DB/function operations). Verified `feat/stripe-billing` at `ea02f0b` contained T1-T5 linearly, not yet merged to `main`. Discovered and fixed a stale/inaccessible Supabase project ref (`wkvattbvybvgaeobtidl`, not in the account's project list) hardcoded in `supabase/config.toml`, `LAUNCH_CHECKLIST.md`, and 6 email template logo/webhook URLs; replaced with the actual active project `gyvjcwuwfwrwwwnuwlex` ("Independent Minds", confirmed via `list_projects`). Fast-forward merged `feat/stripe-billing` into `main` and pushed to `origin/main` (also carried 2 previously-unpushed local `main` commits). Applied 5 migrations to `gyvjcwuwfwrwwwnuwlex` in dependency order: shared `update_updated_at_column()` trigger function and the T4 `20260712173000_launch_ops_gaps` migration (both missing prerequisites discovered because this project had zero tracked migrations despite 29 existing tables), then `20260712190000_stripe_billing_phase1` (`stripe_customers` + `subscriptions`), `20260712193000_stripe_webhook_idempotency`, and `20260712195500_subscriptions_admin_read`. Deployed the 3 Stripe edge functions present in the branch (`create-checkout-session`, `create-portal-session`, `stripe-webhook`) — note: only 3 were found in the branch, not 4.
- Files touched: `supabase/config.toml`, `LAUNCH_CHECKLIST.md`, `supabase/functions/_shared/email-templates/{signup,magic-link,recovery,invite,reauthentication,email-change}.tsx`, `CLAUDE.md`, `docs/ACTIVITY_LOG.md` (git, commit `d600fbc`); remote-only: 5 migrations and 3 edge functions applied directly to project `gyvjcwuwfwrwwwnuwlex` via MCP (not represented as new local migration files since they replay existing ones).
- Validation: `npm run lint` PASS; `npx tsc --noEmit` PASS; `npm run build` PASS (44.52s) — run on the ref-fix commit prior to merge. Post-migration `get_advisors` (security) showed 8 warnings, all pre-existing on functions untouched by this change (`has_role`, `handle_new_user`, `rls_auto_enable`); no new issues introduced.
- Risks + rollback: Git — `main` fast-forwarded from `1e31f38` to `d600fbc`; revert via `git revert` of the range or a hard reset + force-push if truly needed (destructive, requires explicit approval). DB — all 5 migrations are additive only (new tables/columns/policies/function, no drops); rollback = drop `stripe_customers`, `subscriptions`, `billing_events`, `auth_failures`, and function `update_updated_at_column()` if truly unwinding. Edge functions — each deployed as version 1; rollback = delete the function in the dashboard/CLI.
- Blockers/human actions needed: Stripe secrets are still NOT configured on `gyvjcwuwfwrwwwnuwlex` — `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_BASIC`, `STRIPE_PRICE_PLUS`, `STRIPE_PRICE_PRO` (Edge Function secrets) and `VITE_STRIPE_PUBLISHABLE_KEY` (client build env) per `LAUNCH_CHECKLIST.md` section 1 — all 3 deployed functions will error until these are set. Stripe Dashboard product/price/webhook-endpoint setup also still pending. Optionally run `supabase login` locally if a CLI-based workflow (vs. MCP) is preferred going forward.

## 2026-07-12 — T5 Stripe billing (4 phases)
	- Phase 1: `npm run lint` PASS; `npx tsc --noEmit` PASS.
	- Phase 2: `npm run lint` PASS; `npx tsc --noEmit` PASS.
	- Phase 3: `npm run lint` PASS; `npx tsc --noEmit` PASS.
	- Phase 4: `npm run lint` PASS; `npx tsc --noEmit` PASS.
	- Final: `npm run build` PASS; `npm test` PASS (87/87).
	- Phase 1 rollback: revert `20260712190000_stripe_billing_phase1.sql` and `src/hooks/useSubscription.ts`.
	- Phase 2 rollback: revert `create-checkout-session`, `create-portal-session`, `stripe-webhook`, `supabase/config.toml`, and `20260712193000_stripe_webhook_idempotency.sql`.
	- Phase 3 rollback: revert `src/components/SubscriptionGate.tsx`, `src/components/DadPanel.tsx`, and `src/pages/Index.tsx` to remove premium gating.
	- Phase 4 rollback: revert pricing/billing/admin UI files and route/nav wiring (`src/config/plans.ts`, `src/pages/Pricing.tsx`, `src/pages/Billing.tsx`, `src/pages/admin/AdminBilling.tsx`, `src/App.tsx`, `src/components/admin/AdminLayout.tsx`, `src/lib/i18n.tsx`) and remove admin subscription read policy migration `20260712195500_subscriptions_admin_read.sql`.

## 2026-07-12 — T5 Stripe billing finalization and launch checklist
- Summary: Finalized T5 delivery by replacing the root launch checklist with Stripe-specific setup instructions, env-var destinations, and explicit success/failure smoke tests; prepared all T5 code and docs changes for a single structured commit on `feat/stripe-billing`.
- Files touched: `LAUNCH_CHECKLIST.md`, `CLAUDE.md`, `docs/ACTIVITY_LOG.md` plus T5 implementation files across `src/` and `supabase/`.
- Validation: Prior T5 validations remained green (`npm run lint` PASS; `npx tsc --noEmit` PASS after final edits; `npm run build` PASS; `npm test` PASS).
- Risks + rollback: Revert the T5 commit to remove all Stripe billing schema, functions, gating, pricing/billing/admin UI, and launch checklist updates in one step.
- Blockers/human actions needed: Stripe Dashboard setup still required (products/prices, webhook endpoint/events, restricted key, portal config, and env var provisioning).

## 2026-07-12 — T4 Launch ops/legal gaps
- Summary: Added a refund policy page and route, wired lightweight auth-failure telemetry from the login flow into a new edge function and `auth_failures` table, and added hourly-monitor alert scaffolding for auth failure spikes and future Stripe payment failures via a new `billing_events` table.
- Files touched: `src/App.tsx`, `src/pages/Login.tsx`, `src/pages/RefundPolicy.tsx`, `src/lib/i18n.tsx`, `supabase/config.toml`, `supabase/functions/hourly-monitor/index.ts`, `supabase/functions/track-auth-failure/index.ts`, `supabase/migrations/20260712173000_launch_ops_gaps.sql`, `CLAUDE.md`, `docs/ACTIVITY_LOG.md`
- Validation: `npm run lint` PASS; `npx tsc --noEmit` PASS; `npm run build` PASS; `npm test` PASS.
- Risks + rollback: Revert the T4 commit to remove the new legal page, telemetry edge function, alert rules, and schema additions; the migration is additive and isolated to `auth_failures` and `billing_events`.
- Blockers/human actions needed: ES/PT/AR/ZH/DE/JA/RU refund-policy copy currently reuses EN strings by design; replace with localized translations later. Billing webhook population for `billing_events` still depends on T5.

## 2026-07-12 — T3 Enforce sender policy
- Summary: Enforced the queue sender policy by adding an explicit allowlist, warning on disallowed `payload.from` values, and always sending queued mail as `Independent Minds EDU <noreply@independentmindsedu.org>`. Confirmed the ops-only `alerts@notify.independentmindsedu.org` sender used by dns-monitor is present in the allowlist.
- Files touched: `supabase/functions/process-email-queue/index.ts`, `CLAUDE.md`, `docs/ACTIVITY_LOG.md`
- Validation: `npm run lint` PASS; `npx tsc --noEmit` PASS; on-disk sender scan confirmed both the default sender and `Independent Minds EDU Alerts <alerts@notify.independentmindsedu.org>` in the expected files.
- Risks + rollback: Revert the T3 commit to restore queue behavior that forwards `payload.from`; no schema or deployment-side changes were introduced.
- Blockers/human actions needed: None.

## 2026-07-12 — T2 Normalize apex→www
- Summary: Normalized generated auth email template URLs to the canonical `https://www.independentmindsedu.org` value and expanded DNS monitoring to track both the apex domain and the canonical `www` hostname without dropping apex redirect coverage.
- Files touched: `supabase/functions/auth-email-hook/index.ts`, `supabase/functions/dns-monitor/index.ts`, `CLAUDE.md`, `docs/ACTIVITY_LOG.md`
- Validation: `npm run lint` PASS; `npx tsc --noEmit` PASS; local on-disk scan for `https://independentmindsedu.org` inside `supabase/functions/` returned no results.
- Risks + rollback: Revert the T2 commit to restore the previous single-domain DNS monitor and apex-based auth email template URL generation.
- Blockers/human actions needed: None.

## 2026-07-12 — T1 Remove Lovable remnants
- Summary: Removed the remaining scoped Lovable remnants for T1 by deleting the unused Bun lockfile, regenerating npm lock metadata, and replacing the Lovable-hosted social preview image with a self-hosted `og-image.png` copied from the existing PWA asset.
- Files touched: `index.html`, `package-lock.json`, `public/og-image.png`, `bun.lock`, `CLAUDE.md`, `docs/ACTIVITY_LOG.md`
- Validation: `npm run lint` PASS; `npx tsc --noEmit` PASS; `npm run build` PASS; `npm test` PASS (87/87); scoped Lovable grep over `src/ public/ index.html package.json vite.config.ts` returned 0 hits; Vite dev server booted successfully on `http://localhost:8080/`.
- Risks + rollback: Revert the T1 commit to restore the previous lockfiles and OG image metadata; if Bun is reintroduced later, regenerate a fresh `bun.lock` from the new package manager workflow.
- Blockers/human actions needed: None.